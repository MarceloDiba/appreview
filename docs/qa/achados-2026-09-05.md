# Achados de QA — 05/09/2026

Sessão `Binno - QA`. Tudo medido **na produção** (`binno.pro`, Supabase
`tjbznhwdjyabuacrfqie`, Stripe live), com a chave pública do site — a mesma que
qualquer visitante baixa no JavaScript.

**Não repeti** o que a seção 5 do briefing já dá por testado em 04/09 (RLS,
portas sem credencial, 402 de quem não paga, cache do navegador, compra ponta a
ponta com cupom, preço no Stripe).

**Nada foi gasto em API paga.** Nenhuma chamada ao Google Places, OpenAI, Apify
ou Meta. Só bati nas portas para ver recusa.

---

## Quebras

### 1. A política e os termos continuam invisíveis para um robô, no endereço que todo mundo usa

**O que eu fiz**

```
curl -s -o /dev/null -w "%{http_code} %{size_download}\n" https://binno.pro/privacidade
curl -s -o /dev/null -w "%{http_code} %{size_download}\n" https://binno.pro/termos
curl -s -o /dev/null -w "%{http_code} %{size_download}\n" https://binno.pro/privacidade.html
```

**O que aconteceu**

| Endereço | Resposta |
| --- | --- |
| `binno.pro/privacidade` | 200, **1.054 bytes** — a casca vazia do SPA, zero palavras da política |
| `binno.pro/termos` | 200, **1.054 bytes** — idem |
| `binno.pro/privacidade.html` | 200, 12.428 bytes, `<h1>Política de Privacidade</h1>` |
| `binno.pro/termos.html` | 200, 7.786 bytes, `<h1>Termos de Serviço</h1>` |

As páginas estáticas **existem e estão corretas**. Só que moram num endereço que
ninguém usa. Todo link do produto aponta para o outro:

- `src/pages/Index.tsx:228` (rodapé da página de venda) → `/termos`, `/privacidade`
- `src/pages/Signup.tsx:192,196` (o aceite do cadastro) → `/termos`, `/privacidade`
- `src/components/forms/FeedbackForm.tsx:243` (a página do cliente do cliente) → `/privacidade`
- `src/components/layout/LegalLayout.tsx:47,51` → os dois

**Por que importa**

É exatamente o defeito que fez a Meta recusar publicar o app do WhatsApp em
04/09, e que o `check-politica-que-o-robo-le.mjs` foi escrito para impedir. O
WhatsApp oficial é a promessa central da home nova; enquanto a Meta ler uma
casca vazia, ela não sai da análise.

**A causa raiz**, e é uma linha: `vercel.json` (idêntico no `main`) tem um único
`rewrite` de `/(.*)` para `/index.html`. Sem `cleanUrls`, a Vercel não liga
`/privacidade` a `privacidade.html`, então o coringa engole os dois endereços.

**Por que o guarda está verde.** Ele confere que `dist/privacidade.html` existe e
tem política dentro — e tem. Ele nunca pergunta o que o **endereço servido**
devolve. Verde de verdade, protegendo a coisa errada.

Rodei, em vez de deduzir. Com `dist/privacidade.html` e `dist/termos.html`
presentes no disco (logo, as asserções de conteúdo também correram):

```
$ npm run check:politica-que-o-robo-le
Paginas legais que o robo le: 19 protecoes verdes.
EXIT=0
```

Dezenove proteções verdes no mesmo minuto em que `binno.pro/privacidade` serve
1.054 bytes sem uma palavra da política. **Falta a asserção que bate no
endereço** — hoje nenhuma das dezenove sairia vermelha por causa deste defeito.

**Como reproduzir**

```
curl -s https://binno.pro/privacidade | wc -c            # 1054
curl -s https://binno.pro/privacidade | grep -c LGPD     # 0
curl -s https://binno.pro/privacidade.html | grep -c LGPD # > 0
```

---

### 2. Quem pagar e não conseguir ligar o pagamento fica sem acesso e sem para onde ir

**O que eu fiz**

Abri `binno.pro/bem-vindo` num telemóvel (360×780), criei uma conta com um
e-mail descartável e segui até onde a tela me levou.

**O que aconteceu**

A tela mostrou dois avisos e me largou em `/dashboard`:

> Sua conta foi criada, mas não consegui ligar o pagamento a ela. Fale com a
> gente — o pagamento está registrado.

**Por que importa**

Essa é a tela de quem acabou de pagar R$99. Quando a ligação falha:

- O aviso é um *toast*. É a **única** notícia que a pessoa recebe, e ela some.
- Ele diz "fale com a gente" e **não dá endereço nenhum**: nem WhatsApp, nem
  e-mail, nem link. O contato do Binno existe em `src/lib/contactoDoBinno.ts` e
  aparece na página de venda e na página do QR — não aqui.
- Não sai e-mail nenhum. *(Não medi: `RESEND_API_KEY` ausente é o que o briefing
  e o `CLAUDE.md` registram. Não desconfio, então não refiz.)*
- A pessoa cai em `/dashboard`, que é o produto que ela não consegue usar.
  *(Que as sete funções pagas devolvem 402 a quem não assina é o teste de
  04/09, não meu. Não refiz.)*

Ou seja: pagou, não recebeu, e o caminho de volta é uma frase que desaparece.

**Como reproduzir**

1. Abrir `https://binno.pro/bem-vindo` numa janela anónima.
2. Preencher e-mail novo e senha, tocar em "Criar meu acesso".
3. Observar o aviso e o destino.

---

### 3. "Pagamento confirmado" aparece para quem não pagou

**O que eu fiz**

Abri `https://binno.pro/bem-vindo` sem bilhete no endereço, sem sessão e sem
compra nenhuma.

**O que aconteceu**

```
h1        : Pagamento confirmado
subtítulo : Falta só criar o seu acesso.
```

**Por que importa**

O título é fixo em `src/pages/BemVindo.tsx` — não olha para o bilhete, para a
sessão nem para `compras_a_reclamar`. Afirma um fato que não verificou, que é a
invariante da seção 2 do contrato ("nunca apresentar inferência como dado
oficial"). Quem abandonou o checkout e voltou pelo botão do navegador lê que
pagou.

**Como reproduzir**

Abrir `https://binno.pro/bem-vindo` numa janela anónima.

---

### 4. Um QR pode nascer sem destino, e a página do cliente fica só com o canal privado

**O que eu fiz**

Abri a página pública do QR `f1dbfb71-0c24-4dfd-9471-d489c9ea37e4` (ativo,
criado em 04/09 às 22:53) num telemóvel, sem conta.

**O que aconteceu**

A tela inteira:

> Compartilhe sua experiência · Escolha como continuar com **Testando** ·
> **[ Enviar comentário direto ]** · O comentário direto vai apenas para o
> estabelecimento. **A avaliação pública nunca é filtrada.**

Um botão só. Nenhum caminho para o Google. E a frase que promete não filtrar
impressa por baixo.

A causa está no que o endereço público devolve para esse QR:

```
{"business_name":"Testando","google_review_url":null,"tripadvisor_review_url":null}
```

**Não é review gating** — conferi o caso normal. No QR da Noá Digital
(`18b0f6ac-…`), que tem link, as duas portas aparecem lado a lado, sem perguntar
nota:

```
280x48  "Avaliar no Google"  -> https://g.page/r/CbypRGKdQO7eEBM/review
280x48  "Enviar comentário direto"
```

**Por que importa**

Duas coisas, e a segunda é pior que a primeira:

1. O cliente do nosso cliente escaneia o QR e não tem como avaliar no Google.
   É a "página sem nada em que clicar" que o `check-onboarding-que-nao-tranca.mjs`
   nomeia no próprio cabeçalho: *"um QR criado sem endereço leva o cliente a uma
   página sem nada em que clicar. Pior do que não ter QR."*
2. A tela **afirma** que a avaliação pública nunca é filtrada enquanto oferece
   só o canal privado. Quem auditar essa página de fora vê um funil que termina
   em comentário privado com uma frase a dizer o contrário.

**A causa raiz é a regra estar num chamador só.** O guarda prende
`src/pages/Onboarding.tsx`. Mas quem cria QR também é `/qrcodes`, e ali o
`QRCodeGenerator` só pergunta pela origem:

```
src/pages/QRCodes.tsx:57   canCreate={!isLoopback}
```

Li a condição inteira do botão, e não só o nome da prop
(`QRCodeGenerator.tsx:275`):

```js
disabled={ creating || !qrName.trim() || !canCreate
         || (requiresConfirmation && !confirmedNonCanonical) }
```

Origem, nome preenchido e confirmação de origem não canônica. Nada sobre o
Google. E `QRCodes.tsx:31` lê de `profiles` apenas
`business_name, business_country, phone` — a página nunca consulta
`platform_links`, então ela não teria como saber se há link, mesmo que quisesse.
O QR acima é a prova de que esse caminho foi usado.

**Como reproduzir**

```
https://binno.pro/review/f1dbfb71-0c24-4dfd-9471-d489c9ea37e4
```

---

## Riscos

### 5. O cadastro confirma o e-mail sozinho, e `reclamar_compra` confia no e-mail

**O que eu fiz**

```
POST https://tjbznhwdjyabuacrfqie.supabase.co/auth/v1/signup
     apikey: <chave pública do site>
     {"email":"<descartável>","password":"…"}
```

**O que aconteceu**

```
tem sessão          : sim (access_token devolvido na hora)
email_confirmed_at  : 2026-09-05T09:38:12Z
confirmation_sent_at: null
```

Produção confirma o e-mail **no ato**, sem nunca mandar nada para o endereço.
Qualquer pessoa cria conta com qualquer e-mail e sai de lá com sessão viva.

**Por que importa**

Isto sozinho é só o cadastro público aberto, que já é buraco conhecido. O que o
torna caro é a segunda via de `public.reclamar_compra`:

```sql
where reclamada_por is null
  and ( (p_bilhete is not null and stripe_session_id = p_bilhete)
     or (p_bilhete is null and p_email is not null and email = lower(p_email)) )
```

`p_email` vem de `user.email` da sessão — um e-mail que ninguém provou ser da
pessoa. Então:

1. O dono compra em `binno.pro` com `dono@negocio.com`. O webhook grava a linha
   em `compras_a_reclamar`, por reclamar. Conferi o caminho no código, não
   supus: `stripe-billing-webhook/index.ts:71` faz
   `admin.from('compras_a_reclamar').upsert(...)` quando o checkout chega sem
   `user_id` — e o comentário na linha 57 diz que a marca é justamente essa
   ausência.
2. Antes de ele criar a conta, um estranho se cadastra com `dono@negocio.com`.
   Sai com sessão na hora.
3. `/bem-vindo` chama `reclamar-compra`, o e-mail casa, e a assinatura ativa vai
   para a conta do estranho.
4. O dono cria a conta dele e recebe o aviso do achado 2: "não consegui ligar o
   pagamento".

O comentário no topo de `reclamar-compra/index.ts` diz que "quem decide de quem
é a compra é a SESSÃO de quem chama, e nunca o bilhete sozinho". A sessão decide
**quem recebe**, não **quem tem direito** — e com auto-confirmação a sessão não
prova nada sobre o e-mail.

**Hoje não expõe ninguém**: `compras_a_reclamar` está vazia (0 linhas). Vira real
na primeira compra-antes-de-conta, que é o fluxo que a home nova vende.

**Como reproduzir**

Cadastro pelo endereço acima com a chave pública, e ler `email_confirmed_at` na
resposta.

---

### 6. `comprar` cria sessões de checkout live sem nenhuma credencial

`POST /functions/v1/comprar` com corpo vazio e **sem header nenhum** devolve 200
e um `cs_live_…` do Stripe. É por decisão registrada no cabeçalho da função, e o
raciocínio está certo: o corpo é ignorado por inteiro, o preço vem do cofre do
servidor, nada é gravado em banco e as sessões expiram sozinhas.

O que fica em aberto é que não há teto: criei duas num laço, e mil sairiam do
mesmo jeito. Não custa dinheiro, mas suja o painel do Stripe e pode esbarrar no
limite de chamadas dele.

**Como reproduzir**

```
curl -s -X POST https://tjbznhwdjyabuacrfqie.supabase.co/functions/v1/comprar \
  -H "Content-Type: application/json" -d '{}'
```

---

### 7. `auditoria_pro` está no mesmo projeto, dormente e aberta ao anónimo

O esquema `auditoria_pro` divide o projeto Supabase com o Binno. Cinco funções
`security definer` dele são executáveis pelo papel `anon` via
`/rest/v1/rpc/…`, entre elas `auditoria_pro_create_case`, que faz `insert` sem
perguntar nada a ninguém.

A tabela tem 17 linhas, a última de **16/07/2026** — está morta. Quem tiver a
chave pública do site pode encher a tabela. Não vaza dado (as leituras pedem um
token uuid) e não gasta API paga pelo que li, mas é superfície aberta de um
produto que não está em uso.

**Como reproduzir**

`get_advisors` do Supabase, categoria segurança, ou:

```sql
select count(*) from auditoria_pro.cases;   -- 17, último em 16/07/2026
```

---

## Atrito

### 8. Alvos de toque abaixo de 44px em todo o caminho de compra e de acesso

Medido no Chrome a 360×780, com o navegador em pt-BR, contra a produção. A régua
é a do próprio produto: `docs/nova-home-binno.md`, seção 5.2 — *"todo botão
pequeno tem altura mínima de 44px"*.

| Tela | Alvos abaixo de 44px | Os que mais custam |
| --- | --- | --- |
| `/bem-vindo` | **4 de 4** | os dois campos (40px), "Criar meu acesso" (40px), "Continuar com o Google" (38px) |
| `/login` | **8 de 8** | "Entrar" 278×40, "Esqueceu a senha?" 124×20, "Idioma" 40×36 |
| `/signup` | **12 de 12** | "Termos de Serviço" 105×**15**, "Política de Privacidade" 128×**15** — os dois links do aceite |
| `/review/:id` (a página do cliente) | 1 | "Binno · Falar com o Binno" 182×24 |
| `/` e `/demo` | 21 de 34 e 10 de 14 | "Editar" 40px, "Pular" 40px, "Anterior"/"Próxima" 36px |

`/bem-vindo` é o pior lugar para isso: é a tela de quem já pagou.

Os alvos de `/` e `/demo` estão previstos na home nova (`docs/nova-home-binno.md`,
seção 5.2), que ainda está no ramo `feat/home-nova-binno` e não em produção. Os
de `/login`, `/signup`, `/bem-vindo` e `/review` **não estão cobertos por
documento nenhum**.

**Como reproduzir**

Chrome, 360×780, `document.querySelectorAll('a[href],button,input')` e medir
`getBoundingClientRect().height`.

---

### 9. Um bloco passa da borda na página de venda, no celular

Em `/` a 360px, `div.absolute.-inset-10` chega a 384px de direita, com 408px de
largura. Não gera rolagem horizontal (algum ancestral corta), então é cosmético
— mas é o mesmo tipo de vazamento que a seção 5.3 da home nova descreve.

---

## O que conferi e passou

| Testado | Resultado |
| --- | --- |
| Idioma da página de venda | Correto. `pt-BR` → brasileiro, `pt-PT` → português, `en` → inglês. Minha primeira leitura deu inglês porque eu tinha esquecido o `Accept-Language`; refiz. |
| Review gating no QR com link | **Não há.** As duas portas lado a lado, sem perguntar nota. |
| As 13 funções sem `verify_jwt`, sem credencial e com a chave pública | Todas recusam. `oferecer-rascunhos` 403; `publicar-respostas-confirmadas`, `whatsapp-cloud-dispatch`, `telegram-dispatch`, `email-dispatch`, `materialize-whatsapp-notifications`, `apify-auto-collect-on-signup`, `billing-checkout` 401; `whatsapp-cloud-webhook` 401 "invalid signature"; `stripe-billing-webhook` 400. *(Que elas falham fechado com o segredo ausente é leitura do código — `!segredo \|\| header !== segredo` —, não medição: não tenho como tirar o segredo do servidor.)* |
| `saude_das_contas()` | Li a definição **implantada** (`pg_get_functiondef` na produção): confere `public.admins` por dentro e levanta `42501`. Não a chamei como não-admin. O aviso do Supabase é falso positivo. |
| Preços ativos no Stripe live | R$99 (lote de fundadores) e R$129, batendo com `src/lib/precoBinno.ts`. |
| Rolagem horizontal | Nenhuma página tem, em nenhuma das oito testadas. |
| Cadastro nu dispara coleta paga? | Não, e conferi das duas pontas. A definição implantada de `queue_apify_auto_collection_if_ready` sai cedo sem `business_name` e sem link do Google; e depois das minhas duas contas, `apify_auto_collection_queue` continua com **1 linha só**, do dono da Noá — nenhuma minha. |

---

## O que eu criei e o que apaguei

| Criei | Estado |
| --- | --- |
| 2 contas de teste (`qa-binno-…`, `qa-bemvindo-…` em `mailinator.com`) | **Apagadas** de `auth.users`. Voltou a 2 usuários, que era o número de antes. |
| 2 sessões de checkout Stripe live (corpo vazio, ninguém pagou) | **Não consegui expirar** — escrita no Stripe live está bloqueada nesta sessão. Não cobram nada e expiram sozinhas em 24 h. Ids no fim do relatório. |
| 2 aberturas da página pública do QR (Noá e "Testando") | Gravaram 2 `qr_open` em `review_funnel_events`, que foi de 1 para 3. **Apaguei os dois**, pelos ids, às 09:49:26 e 09:50:17. Voltou a 1 linha, a de 04/09 18:01, que não é minha. |

Conferido depois de limpar, e não presumido:

```sql
usuarios=2  contas_de_teste_restantes=0  perfis=2
compras_por_reclamar=0  eventos_de_funil=1 (o mais recente é de 04/09)
apify_auto_collection_queue=1 (do dono da Noá, nenhuma minha)
```

Sessões a expirar, se valer a pena limpar à mão:
`cs_live_a1qqWYQAn5ou9HTxwXTCERfekME0ptT8Cb9brD4fZPBJl5zEmgjynXAoui`
`cs_live_a1LWDBj1QQbWMRMdMpfT7LQOz9GX5uz4gPGdMkgcFJkul3HWqAojauUTZS`

---

# Segunda rodada — 05/09, tarde

Cobre o que o briefing alternativo do Marcelo pedia e o meu não tinha:
**cabeçalhos de segurança, dependências, exposição de segredos e um veredito**.

## Quebra 1 — estado: consertada no ramo, **ainda de pé em produção**

O Assessor consertou em `51dc9d3` (`feat/home-nova-binno`). O `vercel.json` passa
a ter duas regras **antes** do coringa:

```json
{ "source": "/privacidade", "destination": "/privacidade.html" },
{ "source": "/termos",      "destination": "/termos.html" },
{ "source": "/(.*)",        "destination": "/index.html" }
```

**Não acreditei no verde: quebrei a regra três vezes e vi vermelho.** Mutei o
`vercel.json`, rodei o guarda, restaurei — e conferi por `shasum` que o ficheiro
voltou byte a byte (`cd15462b…` antes e depois; `git status` limpo).

| Mutação | O guarda disse |
| --- | --- |
| coringa **primeiro** (a regra existe e nunca corre) | `'/privacidade' vem DEPOIS do coringa; o coringa engole-o antes` (2 falhas) |
| apagar a regra de `/termos` | `'/termos' nao aponta para '/termos.html'` (2 falhas) |
| `/privacidade` a apontar para `/index.html` | `'/privacidade' nao aponta para '/privacidade.html'` (2 falhas) |

O guarda é real, e apanha inclusive o caso da ordem, que é o que *parece*
consertado. Passou de 19 para 23 asserções.

**Mas produção não mudou**, medido depois do commit:

```
/privacidade       200  1054b  LGPD=0
/termos            200  1054b  LGPD=0
/privacidade.html  200 12428b  LGPD=1
```

`git merge-base --is-ancestor 51dc9d3 main` → **não**. O conserto está no ramo. A
quebra continua aberta para quem visita o site agora, e só fecha no merge.

**Uma correção ao meu próprio achado, vinda do Assessor:** no Google Cloud os
dois campos estão cadastrados **com `.html`**, então a quebra 1 **não bloqueia** a
verificação do Google. Não vi o print; é relato dele. O que resta é a Meta
(desconhecido) e — o que vale sozinho — **os links dentro do produto**, que
levavam clientes reais a uma página em branco.

## Risco 10 — seis dos sete cabeçalhos de segurança não existem

**O que eu fiz:** `curl -sI https://binno.pro`.

| Cabeçalho | Estado |
| --- | --- |
| `strict-transport-security` | **tem** (`max-age=63072000`, sem `includeSubDomains` nem `preload`) |
| `content-security-policy` | falta |
| `x-content-type-options` | falta |
| `x-frame-options` | falta |
| `referrer-policy` | falta |
| `permissions-policy` | falta |
| `cross-origin-opener-policy` | falta |

O `vercel.json` só tem `_comentario` e `rewrites` — não há bloco `headers`.

**O que isso alcança de concreto, sem exagerar:**

- **Sem `x-frame-options` nem `frame-ancestors`**, o painel pode ser posto num
  `iframe`. As ações destrutivas dele não são poucas: publicar resposta no perfil
  público do cliente, cancelar assinatura.
- **Sem `referrer-policy` explícita**, fica-se refém do padrão do navegador. E o
  endereço de retorno do Stripe leva o bilhete na barra:
  `success_url: ${appUrl}/bem-vindo?compra={CHECKOUT_SESSION_ID}`
  (`supabase/functions/_shared/billing.ts:153`). O padrão moderno
  (`strict-origin-when-cross-origin`) não manda o caminho para fora, então **hoje
  não vaza** — mas é uma proteção emprestada do navegador, não do produto, e o
  bilhete é meia-chave do risco 5.
- `x-content-type-options` e `permissions-policy` são higiene, custo quase zero.

**Não medi** exploração de nenhum deles. É ausência de defesa, não falha provada.

**Como reproduzir:** `curl -sI https://binno.pro`

## Risco 11 — o router que vai para o navegador tem aviso alto em aberto

**O que eu fiz:** `npm audit --omit=dev`, e depois separei o que chega ao
navegador do que é ferramenta de build.

`react-router-dom@^6.26.2` → `@remix-run/router <=1.23.2`, **high**:

- `GHSA-2w69-qvjg-hvjx` — XSS via open redirect, **CVSS 8.0**
- `GHSA-2j2x-hqr9-3h42` — open redirect com caminho iniciado por `//`
- `GHSA-9jcx-v3wj-wh4m` (6.5), `GHSA-wrjc-x8rr-h8h6`, `GHSA-337j-9hxr-rhxg` (6.1)

`fixAvailable: true` — `npm audit fix` fecha.

**Tentei alcançar e não consegui.** Quatro caminhos, no navegador de verdade:

```
//example.com              -> binno.pro/example.com     ficou no domínio
/\\example.com             -> binno.pro/example.com     ficou no domínio
//example.com/%2f..        -> binno.pro/example.com/…   ficou no domínio
/login?redirectTo=//example.com -> binno.pro/login      ficou no domínio
```

Nenhum saiu do domínio. **Então: a versão vulnerável está publicada, e eu não
achei caminho para explorá-la.** Não provei que é explorável, e não provei que é
segura. Num produto com login pelo Google, um redirect aberto é vetor de
phishing, e o conserto é uma linha de comando.

O resto do `npm audit` (`ws`, `yaml`, `uuid`, `postcss`, `glob`, `lodash`,
`nanoid`, `minimatch`, `brace-expansion`, `picomatch`) é cadeia de build e de
`@supabase/supabase-js`. **Não confirmei quais chegam ao pacote** — o primeiro
teste que escrevi para isso não valia nada (procurava `node_modules/<nome>` num
ficheiro minificado, onde caminho nenhum sobrevive), e não o substituí. Fica dito
como não medido. Total: 27 avisos (17 altos); 16 (11 altos) fora de `devDependencies`.

## O que conferi nesta rodada e passou

| Testado | Resultado |
| --- | --- |
| Segredos no pacote publicado | **Limpo.** Zero ocorrências de `sk_live_`, `sk_test_`, `rk_live_`, `whsec_`, `service_role`, `AIza…`, `apify_api_`, `sk-proj-`, `BINNO_WORKER_SECRET`, `TELEGRAM_BOT_TOKEN`, `GOOGLE_OAUTH_CLIENT_SECRET`, `WHATSAPP_CLOUD_API_TOKEN`. Nenhum JWT. Só a chave publicável do Supabase, que é para estar lá. |
| Source maps | `/assets/index-….js.map` → **403**. |
| Ficheiros que não deviam responder | `.env`, `.env.production`, `.git/config`, `supabase/config.toml`, `package.json`, `vercel.json`: todos 200 **mas com 1.054 bytes** — é o coringa do SPA a devolver a casca, não o ficheiro. Conferi por tamanho e por `id="root"`, não pelo código HTTP. |

*(A primeira versão deste teste de segredos deu "ACHOU" em tudo porque escrevi
`grep -oc`, que não é combinação válida. Refiz com `grep -c`. Fica registado
porque é exatamente o tipo de erro que o briefing manda não esconder.)*

## O que eu não fiz, e por quê

**Não rodei OWASP ZAP nem Nuclei.** Nenhum dos dois está instalado nesta máquina
(`zap.sh`, `zaproxy`, `nuclei`, `nikto`: nenhum). E, mais importante, **não os
rodaria contra `binno.pro` sem falar com o Marcelo primeiro**: a função `comprar`
responde 200 a qualquer `POST` sem credencial e cria uma **sessão de checkout
Stripe live** a cada chamada (risco 6). Um scanner ativo criaria centenas delas,
e provavelmente esbarraria no limite de chamadas do Stripe. Uma varredura ativa
aqui precisa de ambiente separado ou de uma lista de exclusão explícita.

**Não testei** o caminho de compra até o acesso liberado, porque não consigo sem
gastar. É a lacuna que mais pesa no veredito abaixo.

---

# Terceira rodada — 05/09, fim da tarde

## Quebra 1 — rebaixada, com a resposta do Marcelo

**Marcelo confirmou: na Meta o endereço cadastrado é `https://binno.pro/privacidade.html`**,
com `.html`. E o Assessor confirmou o mesmo no Google Cloud (relato dele; não vi
o print).

Então a quebra 1 **não bloqueia** nem a Meta nem o Google. Isso muda a gravidade,
e é justo dizer: o meu enquadramento inicial — *"enquanto a Meta ler uma casca
vazia, ela não sai da análise"* — **estava errado**. Eu supus qual endereço estava
cadastrado, e supor era o que eu não devia ter feito.

**O que sobra, e continua sendo quebra:** todo link dentro do produto aponta para
o endereço sem `.html` — o rodapé da página de venda, o aceite do cadastro
(`Signup.tsx:192,196`) e a página do cliente do cliente
(`FeedbackForm.tsx:243`). Uma pessoa que toque em "Política de Privacidade" antes
de se cadastrar abre uma **página em branco**. Num fluxo de aceite, isso vale o
conserto sozinho, sem precisar de Meta nenhuma.

Consertado em `51dc9d3`, provado por mim com três mutações vermelhas, **ainda no
ramo**.

## Risco 5 — consertado no ramo, e o conserto é o certo

O Assessor fechou em `7f2a0e3`, migração
`20260905090000_so_o_bilhete_reclama_a_compra.sql`. `reclamar_compra` passa a
devolver `null` quando não há bilhete; a via do e-mail deixou de decidir.

Li a migração inteira. O trade-off está escrito no topo, que é o que importa:
quem fechar o navegador antes de criar conta deixa de se reclamar sozinho e passa
a precisar do Marcelo pela porta de administrador. Entre incomodar quem perdeu um
link e deixar roubar assinatura paga por outro, a escolha está certa e está
justificada.

**Ainda no ramo.** Vou provar as duas pontas em produção depois do merge.

## O caminho de compra: até onde deu, e onde parei

**O que eu fiz:** chamei `comprar` uma vez e abri a tela real do Stripe no
navegador, a 360px, em pt-BR.

| Medido | Resultado |
| --- | --- |
| Preço | **R$ 99,00 por mês** — correto |
| Idioma | português |
| Pede e-mail na própria tela | sim |
| Endereço de cobrança | pede, e **Brasil já vem pré-selecionado** (238 países) |
| Rolagem horizontal a 360px | nenhuma |
| **Campo de cupom** | **não existe** |

*(Cheguei a suspeitar que a lista de países fosse atrito — 238 países em ordem
alfabética abrindo em "Afeganistão". Fui ver o valor selecionado antes de
escrever: é `BR`, "Brasil". Não é atrito. Fica registado porque quase virou um
achado falso.)*

### Por que parei: a autorização não cobre o que aconteceria

Marcelo autorizou "compra de teste **com cupom de 100%**". Mas
`createCheckoutSemConta` (`supabase/functions/_shared/billing.ts:145`) não passa
`allow_promotion_codes`, e o padrão do Stripe é falso. **A tela não tem onde
digitar cupom.** A compra sairia a R$99 de verdade, no cartão de alguém.

Não fiz. A autorização era para um teste que o produto, hoje, não permite
executar.

**E isto é um achado por si — atrito 12.** A home nova vende "lote fundador" e
não há como aplicar código promocional no checkout. Para dar desconto a alguém
hoje, seria preciso um link de pagamento à parte. Uma linha
(`allow_promotion_codes: 'true'`) resolve, e é decisão de produto, não minha.

**O que fica por provar, e é a maior lacuna deste relatório:** webhook →
`compras_a_reclamar` → `/bem-vindo` com bilhete de verdade → `reclamar-compra` →
acesso liberado. É onde o dinheiro passa.

## Merge: aprovado pelo Marcelo, encaminhado ao Assessor

Marcelo aprovou o merge do `feat/home-nova-binno`. **Não fiz eu**, por duas
razões: o `AGENTS.md` diz que merge e deploy são dele, e eu não rodei o
`npm run verify` inteiro — quem publica deve ser quem verificou. Encaminhei ao
Assessor com a aprovação.

Deixei um alerta de documento, não de código: `docs/nova-home-binno.md` §7.6
ainda diz "Abrir PR, **sem merge**", e a §8 lista dois impedimentos (adaptador da
Cloud API, e "cancele quando quiser" nunca testado). Os dois parecem resolvidos
pelo `~/binno/CLAUDE.md` de hoje, mas a §8 não foi atualizada. Dois documentos a
discordar sobre se a home pode subir não é coisa que eu deva resolver sozinho.

## Sessões de checkout criadas hoje

Três, todas vazias, ninguém pagou, todas expiram sozinhas em 24 h. Não consigo
expirá-las: escrita no Stripe live está bloqueada nesta sessão, e o Marcelo
confirmou que deixá-las expirar é a decisão certa.

## Nuclei, no ambiente separado — 10 achados, nenhum novo

**O que eu fiz.** Instalei o `nuclei` (v3.11.1, templates v10.4.8), subi o site
já construído num servidor local (`vite preview`, `127.0.0.1:4399`) e varri.
Ambiente separado, como o Marcelo pediu — nada disto tocou `binno.pro`.

```
Templates carregados : 2.949
Pedidos              : 5.881
Duração              : 36 s
Achados              : 10
```

**Os 10 achados são todos o mesmo template**, `http-missing-security-headers`:
CSP, Referrer-Policy, COEP, Permissions-Policy, X-Frame-Options,
X-Content-Type-Options, X-Permitted-Cross-Domain-Policies, COOP, CORP e HSTS.
Todos `info`. Zero em exposição de ficheiros, zero em má configuração, zero em
CVE conhecido.

**Nada aqui é novo:** já tinha medido os cabeçalhos à mão em produção (risco 10).

**E o resultado local sobre cabeçalhos NÃO vale para produção.** O `vite preview`
não serve cabeçalho nenhum; a Vercel serve HSTS. A medição que vale é a que fiz
com `curl -sI https://binno.pro`, onde HSTS existe e faltam os outros seis. Um
scan local que reporta "falta HSTS" está a descrever o servidor de teste, não o
produto.

**O que este scan não conseguiu olhar, e é onde está a superfície real:** as
Edge Functions e o PostgREST do Supabase. O alvo local é um servidor de ficheiros
estáticos — não tem backend atrás. As portas do Supabase eu bati à mão, uma a
uma, o que é mais preciso do que um scanner genérico faria.

**Conclusão honesta sobre o scanner:** custou 36 segundos e não encontrou nada
que a passagem à mão não tivesse encontrado. Isso é informação boa — diz que a
passagem à mão foi completa — mas não substitui varrer o backend, que continua
por fazer e precisa de autorização própria.

---

# Quarta rodada — 05/09, noite

## Quebra 13 — nenhum código promocional é aceito no checkout

**O que eu fiz.** O Assessor ligou `allow_promotion_codes` e criou o cupom
`QACOMPRA0509` (100%, uso único) para eu percorrer a compra. O campo apareceu.
O código foi recusado. Em vez de parar aí, isolei.

**O teste diferencial**, três códigos na mesma sessão de checkout:

| Código | O que é | Resultado |
| --- | --- | --- |
| `QACOMPRA0509` | criado hoje, 100%, ativo, 0 resgates | `Código inválido.` |
| `VALIDACAO3` | já existia, ativo, sem limite de resgates | `Código inválido.` |
| `NAOEXISTE999` | inventado por mim | `Código inválido.` |

Preço fica em R$ 99,00 nos três. **Um código válido e um inexistente dão
exatamente a mesma resposta** — logo o problema não é o cupom.

**Os dois lados estão certos, e mesmo assim não funciona.**

O cupom, lido no Stripe live:

```
coupon bQfoK9QU : percent_off 100, valid true, livemode true,
                  duration once, times_redeemed 0/1, sem applies_to
promo  QACOMPRA0509 : active true, livemode true, expires_at null,
                  0/1 resgates, restrictions todas nulas
```

A sessão de checkout, lida no Stripe live:

```
allow_promotion_codes : true
mode: subscription | currency: brl | amount_total: 9900 | status: open
discounts: []
```

**Por que importa.** Duas coisas:

1. **O teste de compra ponta a ponta continua impossível.** Não há como chegar a
   R$0, e a compra sairia a R$99 reais. Não fiz.
2. **Não existe hoje forma de dar desconto a ninguém.** A home vende "lote
   fundador"; isso hoje funciona porque o preço de R$99 vem do `price_id`, não de
   cupom. Mas no dia em que alguém precisar de um código — um cliente que
   negociou, uma campanha —, o cliente digita e lê "Código inválido" na tela de
   pagamento. Ninguém descobre isso até acontecer com um comprador.

**Hipótese, e é hipótese, não medição.** O Assessor relatou que criar o código
exigiu a forma nova da API (`promotion[type]=coupon` + `promotion[coupon]=<id>`,
porque `coupon` deixou de existir em `promotion_codes`). E a listagem confirma o
objeto guardado nessa forma nova:

```json
"promotion": { "coupon": "bQfoK9QU", "type": "coupon" }
```

Se a sessão de checkout é criada fixada numa versão de API mais antiga, a busca
do código pode não resolver objetos guardados na forma nova. **Não provei isto** —
seria preciso ver a versão de API que o `stripeRequest` usa e compará-la com a da
conta. Fica como a primeira coisa a olhar, não como causa estabelecida.

**Como reproduzir**

1. `curl -s -X POST https://tjbznhwdjyabuacrfqie.supabase.co/functions/v1/comprar -H "Content-Type: application/json" -d '{}'`
2. Abrir a `url` devolvida, tocar em "Adicionar código".
3. Digitar qualquer código — válido ou inventado — e "Aplicar".
4. Ler `Código inválido.` e o preço continuar em R$ 99,00.

## Quebra 1 — reaberta: o merge está no `main`, o deploy não subiu

**O que eu fiz.** O Assessor mergeou (`8437589`) e disse que o deploy da Vercel
corre sozinho. Fui medir.

```
/privacidade       200  1054b  LGPD=0    <- ainda a casca vazia
/termos            200  1054b  LGPD=0
/privacidade.html  200 12428b  LGPD=1
```

**E não é cache.** O pacote publicado continua a ser `index-DzD9IzFu.js`, o
mesmo de manhã. A home nova mexe em 250 linhas de `Index.tsx` e traz um
`HeroAnimado.tsx` novo: se a build nova tivesse subido, o hash mudava à força.

```
last-modified : Sat, 05 Sep 2026 00:16:58 GMT
age           : 37197  (~10 h)
x-vercel-cache: HIT
```

Furei o cache com query aleatória e com `Cache-Control: no-cache` — continua
1.054 bytes. Os três commits estão no `origin/main` (`51dc9d3`, `7f2a0e3`,
`8437589`), então é do lado da Vercel.

**Não consigo ver o painel da Vercel:** a conexão desta sessão lista só o projeto
`auditoria-pro` na equipe `Marcelo Diba`; o `binno.pro` vive em outro âmbito.

É o mesmo padrão do cabeçalho do `check-funcoes-implantadas.mjs`: uma coisa no
repositório e outra no servidor. "O deploy corre sozinho" era a suposição.

**O que fica sem poder ser testado enquanto não subir:** a home nova a 360px, e
os links legais dentro do produto deixarem de abrir página em branco.

## Sessões de checkout criadas hoje

Seis no total, todas vazias, nenhuma paga, todas expiram sozinhas em 24 h.
Continuo sem conseguir expirá-las (escrita no Stripe live bloqueada nesta
sessão), e o Marcelo já confirmou que deixá-las expirar é a decisão certa.

## Achado 14 — a R$0 o Stripe ainda exige cartão, e um cupom `once` cobra no mês seguinte

**O que eu fiz.** O Assessor criou uma sessão de checkout já com 100% de desconto
aplicado pelo servidor, para contornar a quebra 13. Conferi a sessão no Stripe
antes de abrir:

```
amount_total   : 0          amount_subtotal: 9900
mode           : subscription           metadata[sem_conta]: 1
success_url    : https://binno.pro/bem-vindo?compra={CHECKOUT_SESSION_ID}
status         : open       livemode: true
```

Na tela, a 390px: **R$ 0,00**, com R$ 99,00 riscado. Tudo certo.

**O que aconteceu.** Os campos de cartão continuavam lá. Não me contentei em ver
que estavam visíveis — **submeti com o cartão vazio, de propósito**, para separar
exigência de decoração:

```
saiu do Stripe? nao, continua no Stripe
mensagens     : ["OBRIGATÓRIO"]
campo cartao  : {"required": false, "ariaInvalid": "true", "valor": ""}
```

É exigência. Parei aí: não tenho cartão para usar, e não uso o de ninguém.

**A causa, e não é do código do Binno.** O cupom é `duration: once`. A assinatura
fica R$0 no primeiro mês e volta a R$99 no segundo, então o Stripe recolhe meio de
pagamento para a renovação — como deve.

**Por que importa, para além do meu teste.** Se alguém der acesso de cortesia com
um cupom `once`, a pessoa entrega o cartão e **é cobrada R$99 no mês seguinte**.
Quem prometeu grátis descobre pelo lado errado, na fatura do cliente. Para
cortesia de verdade: ou o cupom é `forever`, ou usa-se `acessos_concedidos`, que
já existe e não passa pelo Stripe.

**O que destravaria o teste** (hipótese informada, **não medida por mim neste
projeto**): cupom `duration: forever` **mais** a sessão criada com
`payment_method_collection: 'if_required'`. Uma sozinha não chega — havendo
cobrança futura o Stripe pede cartão de qualquer maneira, e o padrão de
`payment_method_collection` é `always`.

**Nada passou.** Estado do banco antes e depois, idêntico:

```
usuarios=2  compras_por_reclamar=0  assinaturas=1  eventos_webhook=5
```

Não criei conta, não completei compra. A sessão fica aberta e expira sozinha.

**As quatro medições continuam por fazer**, e são as que interessam: a linha
aparecer em `compras_a_reclamar`; o `/bem-vindo` receber o bilhete de verdade; a
conta criada ficar ligada à compra; e a mesma conta passar a 200 onde recebia 402.

## Uma hipótese minha que foi derrubada, e fica registada

Sugeri que a quebra 13 (nenhum código aceito no checkout) fosse a versão da API
do Stripe. O Assessor testou e **não é**: listou o código na versão que o produto
fixa (`2026-07-29.dahlia`) e ele aparece ativo; e criou uma sessão nessa mesma
versão com `discounts[0][promotion_code]`, que **foi aceite, total R$ 0,00**.

Logo cupom, código e versão estão íntegros, e o que falha é especificamente
digitar o código **na tela** do Stripe. A quebra 13 continua aberta, mais estreita
do que eu a descrevi, e sem causa estabelecida.

Fica escrito porque eu propus a hipótese com confiança a mais.

---

# Quinta rodada — o produto visto por quem paga

Marcelo autorizou conceder acesso a uma conta de teste. Criei uma conta
descartável, concedi acesso por `acessos_concedidos` com validade de 2 horas, e
medi o que ninguém tinha medido: **o que existe do outro lado da porta do 402.**

## Quebra 1 — FECHADA, verificada por mim

O deploy falhava por uma razão que só apareceu quando o Marcelo deu acesso à
Vercel ao Assessor: **quatro builds seguidas com ERROR**, por causa do campo
`_comentario` que ele tinha posto no `vercel.json`. A Vercel recusa qualquer
propriedade que não conheça e falha a build inteira. Corrigido em `a4729b7`.

Medido por mim depois do deploy:

```
/privacidade  200  12428 bytes  LGPD=1   h1=Política de Privacidade
/termos       200   7786 bytes  filtrar=2  h1=Termos de Serviço
              e NÃO contém a política por engano (0 ocorrências)
pacote        index-yP_eCoFc.js   (era index-DzD9IzFu.js)
```

**Uma correção à minha própria métrica.** Eu vinha medindo os termos com
`grep -c LGPD`, e isso dá **falso negativo**: LGPD é palavra da política, não dos
termos. Se eu tivesse confiado nela, teria reportado uma quebra inexistente. A
métrica certa para os termos é `grep -c filtrar` — a cláusula que proíbe filtrar
avaliações, que é a que não pode desaparecer de lá. Foi o Assessor que apanhou.

**E o `verify` esteve verde durante as quatro builds falhadas**, porque lia o JSON
e nunca perguntou se a Vercel o aceitaria. O guarda ganhou a asserção que faltava.

## Quebra 15 — "1 esperando resposta" numa fila vazia, só em português do Brasil

**O que eu fiz.** Abri `/reviews` com a conta de teste, recém-criada, com zero
dados de qualquer espécie.

**O que aconteceu.** A mesma tela diz as duas coisas:

```
Fila de respostas
1 esperando resposta          <- o crachá
Mensagens privadas    0
Comentários no Google 0
Nada esperando resposta agora <- o estado vazio, logo abaixo
```

**Não é vazamento de dados.** Procurei nomes das outras contas na página —
"Noá", "Mesquita", "Daniel", "Testando", "Bistrô", "Luciano", "H5 TEXAS" — e
nenhum aparece. O `localStorage` tem só `binno.dono-deste-aparelho` e o token de
sessão. O isolamento entre contas continua de pé, agora medido também **com o
402 aberto**, que era o lado que faltava.

**A causa raiz, provada e não deduzida.** O crachá usa a lista certa
(`FilaDeRespostas.tsx:497` passa `count: filaVisivel.length`, que é zero). O
defeito está no catálogo, e é uma regra de plural:

```
Intl.PluralRules('pt-BR').select(0)  ->  "one"     <- zero é SINGULAR em pt-BR
Intl.PluralRules('pt-PT').select(0)  ->  "other"
Intl.PluralRules('en').select(0)     ->  "other"
```

E a forma singular do pt-BR tem o número **escrito à mão**:

```json
"pending_one":   "1 esperando resposta",
"pending_other": "{{count}} esperando resposta"
```

Com zero itens, o pt-BR escolhe `_one` e imprime o "1" literal. Em pt-PT e em
inglês o mesmo código escolhe `_other` e imprime "0". **O defeito só aparece na
língua do mercado principal.**

**Por que importa.** É a classe de defeito que o contrato persegue em três
lugares: as duas superfícies não podem discordar, a contagem tem de nascer da
mesma lista, e *"um número que nunca desce ensina o dono a ignorar o número"*.
Aqui o número nunca desce abaixo de 1, e a frase logo abaixo dele diz o
contrário, na mesma dobra.

**E não é uma chave só.** Varri o catálogo pt-BR: das 9 chaves `_one`, **6 não
usam `{{count}}`** e portanto imprimem um número fixo quando a contagem é zero:

| Chave | O que passa a dizer com zero |
| --- | --- |
| `reviews.queue.pending_one` | "1 esperando resposta" — **confirmado em produção** |
| `dashboard.radarLive.priority.title_one` | "1 avaliação aguarda resposta" |
| `dashboard.cockpit.approved.distributionTooFew_one` | "Com 1 avaliação buscada…" |
| `dashboard.cockpit.approved.volumeThisWeek_one` | "avaliação nesta semana" |
| `dashboard.cockpit.approved.waitingReplies_one` | "avaliação espera resposta" |
| `dashboard.reviewQueue.completedBody_one` | "…e 1 deixada para depois" |

A primeira eu vi na tela. As outras cinco saem da mesma construção; as três
primeiras têm número fixo no texto e são do mesmo tipo, as duas últimas dependem
de como o componente desenha a contagem ao lado e **não confirmei numa tela**.

**Como reproduzir**

1. Conta nova, sem dados, com acesso liberado.
2. Abrir `https://binno.pro/reviews` com o painel em português do Brasil.
3. Ler o crachá e o estado vazio na mesma tela.
4. `node -e "console.log(new Intl.PluralRules('pt-BR').select(0))"` → `one`.

## O que passou, medido do lado de dentro

| Testado | Resultado |
| --- | --- |
| **Nenhuma função paga dispara sozinha** | Monitorei a rede nas seis telas. **Zero** chamadas a `fetch-google-reviews`, `sync-experimental-apify`, `sugerir-resposta`, `temas-das-avaliacoes`, `search-prospects` ou `sync-google-business-profile`. Abrir o produto não gasta dinheiro. |
| O portão abre para quem tem acesso | Antes da concessão: 402 `SEM_ASSINATURA` em 6 portas. Depois: cada uma falha na **própria validação** — 403 sem link do Google, 422 sem link público, 422 `SEM_COMENTARIO`, 422 `POUCO_TEXTO`, 409 `GOOGLE_CONNECTION_REQUIRED`. `tem_acesso` funciona nas duas direções, que era o risco escrito no cabeçalho do guarda: trancar por fora seria pior. |
| `search-prospects` | 401 mesmo com acesso — é só do dono da casa, como decidido. |
| `billing-checkout` | 422 de validação, nunca 402. Exigir pagamento para poder pagar continua proibido, e continua respeitado. |
| Isolamento com o 402 aberto | Nenhum nome de outra conta em nenhuma das seis telas. |
| Rolagem horizontal a 390px | **Nenhuma**, nas seis telas autenticadas. |

## Atrito 8 — alcance maior do que eu tinha medido

Com a conta de teste dá para medir as telas de dentro. Alvos abaixo de 44px de
altura, a 390px:

| Tela | Alvos pequenos |
| --- | --- |
| `/whatsapp` | **17 de 17** |
| `/settings` | **14 de 14** |
| `/profile` | **14 de 14** |
| `/qrcodes` | 8 de 8 |
| `/dashboard` | 6 de 6 |
| `/reviews` | 5 de 7 |

Não é uma tela: é o produto inteiro. A régua continua a ser a do próprio produto,
44px, escrita em `docs/nova-home-binno.md`.

---

# Sexta rodada — a aba de cobrança de quem tem acesso concedido

O Assessor pediu esta, e suspeitava de duas saídas ruins: erro cru na tela, ou —
pior — "assinatura ativa" com um botão de portal que não abre. **Nenhuma das
duas aconteceu.** O que existe é outra coisa.

## O que a tela faz, com acesso concedido e zero assinatura no Stripe

Conta nova, `acessos_concedidos` com validade de 1 hora, nenhuma assinatura no
Stripe. Confirmado no banco antes de olhar a tela:

```
tem_acesso            : true
acesso_expira_em      : 2026-09-05 12:07:15+00
assinaturas_no_stripe : 0
```

`/profile?aba=assinatura` abre, na aba certa, e diz:

> **Assinatura** — Como está a sua conta hoje. Informe o país onde seu negócio
> opera na aba Perfil antes de continuar. Sem período mínimo de fidelidade.

E a função responde honestamente:

```
billing-checkout (action: status) -> 200
{"subscription":null,"billing":{"businessCountry":null,"market":null,"available":false}}
```

**O que passou:** a aba é alcançável (o guarda `check-a-assinatura-tem-porta.mjs`
faz o trabalho dele), não há erro cru, não há botão de portal a prometer o que
não pode cumprir, e **em nenhum momento a tela afirma uma assinatura que não
existe**. A suspeita pior do Assessor não se confirma.

## Atrito 16 — quem tem acesso concedido não vê o acesso em lado nenhum, nem quando ele acaba

**O que aconteceu.** A conta **tem o produto liberado** — os 402 abrem, as telas
funcionam, `tem_acesso` devolve `true`. E a aba que existe justamente para
responder "como está a minha conta" não diz nada disso. Diz uma instrução de
configuração, como se a pessoa ainda não tivesse começado.

**Por que importa.** Esta não é uma situação de laboratório: é exatamente o que o
Marcelo autorizou hoje — *"podemos dar acesso a uma conta teste"* — e é a forma
que o produto tem de dar cortesia sem passar pelo Stripe. Quem receber acesso
assim abre a aba Assinatura e não encontra:

- que tem acesso;
- **até quando** — e `acessos_concedidos.expira_em` existe e estava preenchido.
  O meu expirava às 12:07. No dia em que expirar, o produto fecha sem a pessoa
  ter tido onde ler a data.

É a mesma queixa que fez nascer o guarda da porta da assinatura, em 04/09, nas
palavras do Marcelo: *"dentro do app não tem nada relacionado a assinatura. Logo,
não se sabe se está ativa, até quando é válida, quando renova"*. Para quem paga
pelo Stripe isso foi resolvido. **Para quem tem acesso concedido continua inteiro**,
e o caminho de acesso concedido é mais recente que o guarda.

**Como reproduzir**

1. Conta nova; inserir linha em `acessos_concedidos` com `expira_em` no futuro.
2. Entrar e abrir `https://binno.pro/profile?aba=assinatura`.
3. A tela pede o país e não menciona o acesso nem a data em que ele termina.

## Quebra 15 (plural) — consertada no ramo, **não em produção**

O Assessor consertou as quatro chaves nos três catálogos (`336c98e`) e escreveu
um guarda que mede a própria regra de plural — se o `Intl` mudar de ideia sobre o
português do Brasil, o guarda fica vermelho dizendo que a razão dele mudou.

Medido por mim:

```
336c98e está em origin/main?   NAO   (está em feat/home-nova-binno)
main local == origin/main      sim, ambos em a4729b7
pacote no ar                   index-yP_eCoFc.js  (inalterado)
```

O ficheiro no disco já está certo nos três idiomas (`"{{count}} esperando
resposta"`), mas **a produção continua a dizer "1 esperando resposta" numa fila
vazia**. Fica por verificar no ar.

É a terceira vez hoje que "está feito" e "está no ar" divergem: o `_comentario`
que derrubou quatro builds, o merge que não deployou, e agora um commit no ramo
descrito como estando no `main`. Não é descuido de ninguém em particular — é que
o repositório e o servidor são duas perguntas diferentes, e só a segunda importa
para o cliente.

---

# Sétima rodada — a superfície pública do QR

## Quebra 15 (plural) — FECHADA, verificada em produção

```
pacote no ar                        index-UXzQOQSs.js  (era yP_eCoFc)
useOwnerTranslation-Dh2IF8Z2.js     237.400 bytes
  contém "{{count}} esperando resposta"   1
  contém "1 esperando resposta"           0
```

## Quebra 17 — o cliente que só escreve um elogio é gravado como nota 3, e o dono recebe alerta vermelho

**Esta é a mais séria do dia.** Cada elo foi medido, nenhum deduzido.

### O que eu fiz

Percorri o caminho real de um cliente, num telemóvel: abri o QR da Noá em
`/review/…`, toquei em **"Enviar comentário direto"**, e olhei para o formulário.
Depois fiz o mesmo por link direto, para comparar.

### O que aconteceu

**No caminho real, três das cinco estrelas já vêm acesas.** Medido pelo
preenchimento do SVG na tela:

```
caminho REAL (QR -> "Enviar comentário direto")
   estrela 1  fill = rgb(250,204,21)   <- amarela
   estrela 2  fill = rgb(250,204,21)   <- amarela
   estrela 3  fill = rgb(250,204,21)   <- amarela
   estrela 4  fill = rgb(209,213,219)  cinzenta
   estrela 5  fill = rgb(209,213,219)  cinzenta

link DIRETO (/feedback/<id>)
   as cinco cinzentas                  <- nenhuma nota assumida
```

### A cadeia, elo a elo

1. `src/components/review-funnel/ReviewChooser.tsx:56` navega para o formulário
   com `state: { rating: 'neutral', … }` — **escrito à mão, sem o cliente ter
   escolhido nada.**
2. `notaDoRating('neutral')` devolve **3** (`src/lib/comentarioInterno.ts`).
3. `FeedbackForm.tsx:65` arranca com `useState(notaDoRating(rating))` — daí as
   três estrelas acesas.
4. O gatilho implantado, lido da produção:
   ```sql
   if new.rating is null then return new; end if;
   if new.rating <= 3 then especie := 'feedback'; janela := interval '5 minutes';
   ```
   e a mensagem que sai:
   ```
   🔴 *Comentário privado agora*
   Deixou *nota 3 de 5*.
   💬 "…o que o cliente escreveu…"
   ```

### Por que importa

Um cliente escaneia o QR, toca em "Enviar comentário direto", escreve *"adorei,
atendimento ótimo"* e envia **sem tocar nas estrelas**. O produto:

- grava **nota 3** como se fosse a opinião dele;
- manda ao dono um alerta **vermelho** de reclamação, em 5 minutos, dizendo
  *"Deixou nota 3 de 5"* — com o elogio citado logo abaixo.

O dono é avisado de um cliente insatisfeito que não existe. E há uma segunda
consequência, mais silenciosa: **a nota média interna do negócio é puxada para
baixo por notas que ninguém deu.**

### É exactamente o defeito que um guarda deste repositório descreve

`src/lib/comentarioInterno.ts`, no comentário da própria função:

> *"Assumir 3 punha na boca do cliente uma opinião que ele nunca deu: 3 é nota
> baixa, fica gravada como avaliação dele e dispara o aviso de reclamação no
> WhatsApp do dono, que era avisado de um cliente insatisfeito inexistente."*

E o cabeçalho de `scripts/check-nota-honesta.mjs` diz que o defeito era
`Feedback.tsx` assumir `'neutral'` **"quando a pessoa chegava ao formulário sem
ter escolhido nada (link direto, refresh, voltar do navegador)"**.

**O conserto tratou o caso raro e deixou o caso comum.** O link direto — que
quase ninguém usa — ficou correcto: cinco estrelas cinzentas. O caminho do QR,
que é o único que um cliente real percorre, passou a injectar o `'neutral'` a
partir do chamador. O módulo recusa-se a assumir 3; o chamador entrega-lhe 3.

**É a mesma forma da quebra 4:** a regra vive num sítio e o chamador que
importa não passa por lá. O guarda mede `comentarioInterno.ts`, `Feedback.tsx` e
`FeedbackForm.tsx` — e **não mede `ReviewChooser.tsx`**, que é a única tela que
navega para ali.

### E os dados concordam

```
internal_feedback : 12 linhas
  com nota        : 12      sem nota: 0
  nota <= 3       : 10
```

Nenhuma linha sem nota, em nenhuma conta, desde 31/07. Não provei que as dez
notas baixas sejam todas falsas — não sei o que cada cliente sentiu. O que está
provado é que **o produto marca 3 antes de o cliente tocar em nada**.

### Como reproduzir

1. `https://binno.pro/review/18b0f6ac-cb72-4c21-aa05-f1159e6572d0` num telemóvel.
2. Tocar em **"Enviar comentário direto"**.
3. Contar as estrelas acesas: **três**.
4. Comparar com `https://binno.pro/feedback/18b0f6ac-cb72-4c21-aa05-f1159e6572d0`,
   onde nenhuma acende.

**O que eu NÃO fiz:** não enviei o formulário. Enviar criaria uma linha na conta
de um negócio real e dispararia um aviso ao dono. Precisa de autorização.

## Quebra 1 — confirmada fechada na tela do cliente

O link "Como seus dados são tratados", no formulário que o cliente do nosso
cliente vê, aponta para `/privacidade` e agora responde **200 com 12.163 bytes e
a política dentro**. Era esta a superfície que mais custava: um cliente a tocar
no link de privacidade antes de escrever, e a abrir uma página em branco.

## Atrito 8 na superfície pública

| Tela | Alvos abaixo de 44px |
| --- | --- |
| `/review` | "Binno · Falar com o Binno" 182×24 |
| `/feedback` | o **X de fechar, 20×20**; nome 276×40; indicativo 87×40; WhatsApp 187×40; "Como seus dados são tratados" 255×35; "Avaliar no Google" 276×38 |

Os dois botões grandes de `/review` (310×48) e o "Enviar" (276×48) estão certos.
O X de 20×20 é o menor alvo que encontrei no produto inteiro.

Nenhuma das quatro telas públicas tem rolagem horizontal a 390px.

---

# Oitava rodada — a área de administrador

O contrato tem regras específicas para ela, e todas passam.

| Testado | Resultado |
| --- | --- |
| `/admin` sem sessão nenhuma | **404 "Page not found"**. Não redireciona para o login, não diz "acesso negado", não mostra lista vazia. É exactamente o que o contrato exige: *"uma negação confirma que a rota existe, e uma rota que se sabe existir é uma rota que alguém tenta"*. |
| `saude_das_contas` sem credencial | 401 `No API key found` |
| `saude_das_contas` com a chave pública do site | 401 `42501 permission denied for function` |
| `saude_das_contas` com sessão de conta comum | **403 `42501 nao autorizado`** — a verificação de `public.admins` dentro da função dispara |
| Lista de funções do PostgREST ao anónimo | **401.** O esquema não é enumerável: quem não sabe o nome de uma função não o descobre por aqui |

Nenhuma das quatro portas devolve dado. E a regra que o contrato mais teme —
*"uma lista vazia nunca pode ser a resposta a quem não tem permissão"* — não é
violada: nenhuma delas devolve lista nenhuma.

## Atrito 18 — a página 404 está em inglês, e não é traduzida

**O que eu fiz.** Abri `binno.pro/admin` com o navegador em pt-BR.

**O que aconteceu.**

> **404** — Oops! Page not found — Return to Home

**A causa, lida no ficheiro.** `src/pages/NotFound.tsx` tem os três textos
escritos à mão em inglês. Não usa `useOwnerTranslation` nem o dicionário do
cliente; não há chave nenhuma para traduzir.

**Por que importa.** É a tela que aparece a quem erra um endereço, e o mercado é
Brasil primeiro. Também é o que vi hoje em `/cadastro` e `/precos`, dois
endereços que não existem e que uma pessoa tentaria por adivinhação — a rota de
cadastro é `/signup`.

Fica também registado que ela escreve `console.error` a cada 404, o que enche o
console do navegador do cliente sem servir para nada em produção.

**Como reproduzir:** abrir `https://binno.pro/qualquer-coisa` com o navegador em
português.

---

# A decisão da nota assumida (quebra 17) — fechada em A

Marcelo passou a decisão para nós: *"Se vocês entraram em acordo, eu concordo."*

**Ficou A:** o formulário abre sem estrela nenhuma, e quem escreve sem dar nota
**gera aviso mesmo assim** — com uma espécie nova, `feedback-sem-nota`, que usa
💬 sem cor, diz "o cliente escreveu sem dar nota", mostra o texto e **nunca
escreve "nota X de 5"**. Sem nota e sem texto continua a não avisar ninguém.

## A dúvida que decidiu, e como foi medida em vez de opinada

O Assessor levantou o contra-argumento certo a A: *e se quase toda a gente que
escreve sem nota estiver a elogiar? Aí A vira ruído, o dono deixa de ler os
avisos, e isso é pior do que não avisar.* E disse não haver dados, porque as 12
linhas existentes têm todas nota.

**Há dados, e estavam escondidos na própria distribuição.** O valor assumido era
3. Logo, qualquer nota **diferente de 3** só pode ter vindo de um toque
deliberado do cliente:

```
nota 1 : 4 linhas   <- tocou nas estrelas
nota 2 : 2 linhas   <- tocou
nota 3 : 4 linhas   <- ambíguo: toque ou o valor assumido
nota 5 : 2 linhas   <- tocou
total  : 12, todas com texto
```

**Pelo menos 8 de 12 tocaram nas estrelas; no máximo 4 não tocaram.** No pior
caso, um terço dos comentários chega sem nota — e é esse terço o volume extra de
avisos que A cria, já limitado pelo tecto diário de 5. Sob B, esse mesmo terço
seria queixa perdida, sem tecto e sem aviso.

**A assimetria fecha a decisão:** o erro de A é um aviso a mais, que o dono lê e
descarta em dois segundos. O erro de B é uma queixa que não chega, num produto
cujo pilar vendido na home é "não seja pego de surpresa".

## Onde esta medição é fraca, dito antes de alguém perguntar

**As 12 linhas somos praticamente nós.** O produto não tem cliente pagante, e
quem escreveu foi quem estava a testar. Não é amostra de comportamento de cliente
real. Ela não prova que A é seguro — prova apenas que a hipótese contrária
("quase toda a gente escreve sem nota") não está sustentada por nada que
tenhamos.

## O fio de arame, para isto não virar opinião congelada

Quando houver cliente real, a pergunta responde-se sozinha:

```sql
select count(*) filter (where rating is null) as sem_nota,
       count(*) filter (where rating is not null) as com_nota
  from public.internal_feedback
 where created_at > <data do primeiro cliente>;
```

Se `sem_nota` passar a dominar, A virou ruído e a decisão deve ser revista. Uma
decisão tomada com dados fracos tem de dizer o que a faria mudar de ideia, senão
daqui a três meses ninguém se lembra de que foi tomada assim.

## As 12 linhas antigas ficam como estão

Algumas podem carregar o 3 assumido em vez da opinião do cliente, e não há como
distinguir olhando a linha. Assessor e QA concordam em não mexer: reescrever
histórico de cliente com base numa suposição sobre o que a pessoa quis dizer é o
mesmo erro que se está a corrigir, ao contrário.

## O que fica por medir quando o conserto subir

1. No caminho real do QR, as cinco estrelas nascem cinzentas.
2. Comentário enviado sem tocar nas estrelas grava `rating = null`.
3. Esse comentário gera linha em `whatsapp_outbox` com espécie `feedback-sem-nota`.
4. O texto desse aviso **não** contém "nota" nem "de 5".
5. O bloco "Comentários que pedem atenção" continua a aparecer para um comentário
   sem nota — ele lê `is_addressed`, não `rating`. Se mudar, é regressão.

---

# Nona rodada — o WhatsApp oficial, e a medição 5

## Risco 19 — o canal oficial do WhatsApp nunca passa de `accepted`

**O que eu fiz.** Contei a `whatsapp_outbox` por fornecedor e estado.

```
meta-cloud : accepted 3   delivered 0   read 0     (04/09)
openwa     : accepted 1   delivered 7   failed 3   (29/08 a 31/08)
telegram   : accepted 13  failed 1                 (31/08 a 04/09)
```

**O canal morto tinha confirmação de entrega. O canal oficial não tem.**

**A causa, e é um grep vazio, não interpretação minha.**
`supabase/functions/whatsapp-cloud-webhook/index.ts:244` lê apenas
`valor.messages`. O ficheiro não contém uma única ocorrência de `statuses`,
`delivered`, `read` ou `ack` — e `statuses` é o campo onde a Meta entrega os
recibos de `sent`, `delivered` e `read`.

**Por que importa.** O contrato constrói a honestidade da tela de WhatsApp sobre
a diferença entre estes estados:

> *"a tela só diz 'ligação ativa' quando a última mensagem de teste chegou ao
> estado `accepted`, `delivered` ou `read`"*

e escreveu, na auditoria de 31/08, o risco residual de aceitar `accepted`:

> *"uma sessão despareada que ainda responda 200 nesse endpoint seria lida como
> ligação ativa"*

**Deixou de ser residual.** No canal oficial, `accepted` não é um dos três
estados possíveis: é o único. O produto não consegue distinguir "a Meta aceitou a
chamada" de "a mensagem chegou ao telemóvel do dono", e a janela de prova de sete
dias mede uma coisa que nunca é confirmada.

É também regressão face ao OpenWA, que confirmava entrega e foi substituído.

**Não é quebra:** ninguém fica sem usar o produto por causa disto. É risco.

**Como reproduzir**

```sql
select provider, status, count(*) from public.whatsapp_outbox group by 1,2;
```
e `grep -c "statuses" supabase/functions/whatsapp-cloud-webhook/index.ts` → 0.

## O número de teste da Meta — não é mensurável daqui, e sei dizer porquê

O Assessor perguntou se o número oficial ainda é número de teste, limitado a 5
destinatários pré-registados.

**Não dá para responder pelo banco, e o silêncio é igual nos dois estados.** Todas
as mensagens que o produto já enviou, em todos os canais e em toda a história,
foram para **um único destinatário**. Nunca houve uma tentativa para outro número.
Logo não existe — e não poderia existir — um erro do tipo "destinatário fora da
lista permitida": esse erro só apareceria numa tentativa que nunca foi feita. Os
únicos erros registados são `OpenWA 409` e `fetch failed`, ambos do canal morto.

Só há dois caminhos: o painel da Meta, ou um envio a um segundo número — que as
regras desta sessão proíbem sem autorização expressa.

## Medição 5 — a fila passa; o painel ficou por medir

Conta de teste com um comentário de `rating = null` e texto. Em `/reviews`:

```
Fila de respostas   1 esperando resposta        <- contagem certa
Mensagens privadas  1     Comentários no Google 0
Comentário privado · QA · 5 de set. de 2026
"Cliente não deu nota"                          <- estado honesto
[texto]   Sugerir resposta   Marcar como resolvido
```

Nenhuma estrela desenhada e a frase certa. O plural também se confirma aqui: com
um item real, o crachá diz 1.

**No `/dashboard` não consegui medir**, e não é regressão: a conta não tem
configuração terminada, e o painel mostra "Falta terminar a configuração" em vez
do cockpit. O bloco não aparece porque o cockpit inteiro não aparece. Fica
**não medido**.

## Um quase-acidente, registado porque quase custou dinheiro

Para completar a configuração da conta de teste, inseri um `platform_links` com
URL do Google, **convencido de que o gatilho da coleta paga vivia só na tabela
`profiles`** — foi o que eu tinha listado horas antes.

Medi a fila antes e depois, por hábito. Subiu de **1 para 2**: há gatilho em
`platform_links` também. Apaguei a linha e o pedido em segundos e confirmei que
`experimental_apify_runs` continua em 2, nenhuma minha. **Zero gasto — mas por
pouco, e só porque medi em vez de supor.**

**Consequência para quem vier a seguir:** não dá para ter uma conta de teste
totalmente configurada sem enfileirar uma coleta paga da Apify. Medir o cockpit
inteiro exige um interruptor ou autorização expressa.

## Um falso positivo que eu não mandei

Ao conferir o conserto A, o gatilho ainda continha `if new.rating is null then` —
a saída antecipada antiga. Quase reportei que ela tinha sobrevivido ao conserto.
Fui ler o trecho inteiro antes de escrever: foi **reaproveitada**, não deixada
solta — sem texto devolve `new`, com texto vira `feedback-sem-nota` com janela de
15 minutos. Está correcto.

Fica registado porque é o tipo de erro que este relatório existe para não cometer:
uma expressão a bater num ficheiro não é a leitura do que o código faz.

---

# Décima rodada — verificação do conserto dos recibos

## Risco 19 — consertado, e verificado no servidor

Não aceitei o relato. Conferi as quatro coisas que podiam estar erradas:

| Conferido | Resultado |
| --- | --- |
| `42cd560` está no `origin/main`? | **Sim** |
| A função está **implantada**, e não só comitada? | `whatsapp-cloud-webhook` passou da **versão 14 para a 16**, `updated_at` de hoje |
| O que está **no servidor** tem o conserto? | Li o código implantado pela API do Supabase: contém `recibos.push(...(valor.statuses || []))`, a escada `['queued','sending','accepted','delivered','read']`, e a regra do `failed` |
| O `tsc` do projeto cobria as funções? | **Não.** `tsconfig.app.json` tem `"include": ["src"]`. São **25 ficheiros** a correr em Deno sem verificação nenhuma |

As três regras difíceis estão lá, e são as certas: o recibo nunca recua (a Meta
não garante ordem, e um `delivered` atrasado depois do `read` apagaria a
informação melhor); o `failed` é terminal mas não apaga uma entrega já
confirmada; e um recibo sem linha correspondente não é erro, é anotado e
seguido.

**O que fica por provar, e só uma mensagem real prova:** que os recibos chegam e
avançam a linha. Verifiquei que o código está no servidor e que sabe tratá-los;
não verifiquei um `delivered` a acontecer, porque isso exige enviar uma mensagem
de WhatsApp.

**E as 3 linhas antigas ficam em `accepted` para sempre** — a Meta não reenvia
recibos de mensagens passadas. Quem olhar a tabela amanhã não deve ler isso como
o defeito a persistir.

## O guarda novo — provei vermelho com o defeito real, não com um parecido

O Assessor avisou que a primeira versão do guarda dele ficou **verde com o
defeito real reposto**: filtrava só `TS2304`, e o TypeScript troca para `TS2552`
quando consegue sugerir um nome parecido. Havia um `Text` no lib do DOM, então o
erro do `texto` saía como TS2552 e passava.

Fui verificar a correção repondo o defeito exacto, letra por letra:

```
--- MUTACAO: dito.slice -> texto.slice   (o defeito que esteve em produção)
    error TS2552: Cannot find name 'texto'. Did you mean 'Text'?     -> VERMELHO

--- MUTACAO: dito.slice -> zzqqxx.slice  (nome sem nada parecido)
    error TS2304: Cannot find name 'zzqqxx'.                         -> VERMELHO
```

Restaurei e conferi por `shasum`: `ec1e58e8…` antes e depois, `git status` limpo.

**Os dois códigos de erro são apanhados agora**, e o que importa é que o caso
real é o TS2552 — a metade que faltava era exactamente a que o defeito de
produção produzia. Um guarda que protege contra metade de um defeito parece
proteção e não é.

Passa em 25 ficheiros e está no `verify`.

## O defeito que o conserto destapou

`console.error(..., texto.slice(0, 20))` numa função onde a variável se chama
`dito`. Um `ReferenceError` à espera do dia em que um dono confirmasse sem haver
nada pendente: o pedido rebentava, a Meta recebia 500 e **voltava a tentar o
mesmo evento** — repetição de confirmação, que é o sintoma que aquela função
inteira foi desenhada para evitar.

Esteve em produção durante dias. O defeito não era a linha: era não haver nada a
olhar para ali.

---

# O número da Meta — respondido pelo painel, com o que ainda falta

Marcelo mandou dois prints. **Isto é leitura de imagem, não medição minha** — vale
como evidência, de espécie diferente do resto deste relatório.

## O que os prints resolvem

**Não é número de teste.** O WhatsApp Manager mostra:

```
+55 79 9198-6091  ·  Brasil
Nome    : Binno   (Em revisão)
Estado  : Ligado
Qualidade: Elevada
```

Um número próprio, ligado e com qualidade elevada. Os números de teste que a Meta
oferece são americanos (+1), emitidos por ela — este é o do Marcelo, o mesmo que
era do piloto OpenWA.

**Não há lista de 5 destinatários.** No `API Setup`, o campo **To** é um seletor
de país mais uma caixa de texto livre. Com um número de teste da Meta, esse campo
é uma lista fechada com "Manage phone number list" e no máximo 5 entradas. Não é
o caso. A hipótese que o Assessor levantou fica **descartada**.

O próprio texto da página confirma o enquadramento: *"Test phone numbers allow you
to send free messages for 90 days. **You can use your own phone number, which is
subject to limits and pricing.**"* — o Binno está no segundo caso.

## O que fica pendente, e é outra coisa

**O nome de exibição "Binno" está "Em revisão".** Não impede enviar; impede o nome
verificado aparecer a quem recebe.

## O que os prints NÃO mostram, e ainda decide o alcance

1. **O limite de mensagens** — o número de clientes distintos por dia (250, 1 mil,
   10 mil). Não aparece em nenhum dos dois prints, e é ele que diz a quantos
   clientes o produto chega.
2. **Se o app está "Live" ou "Em desenvolvimento".** Um app em desenvolvimento
   restringe o envio a testadores, independentemente de o número ser real.
3. **A verificação do negócio** (Business Verification), que é o que destranca os
   escalões acima do inicial.

## Uma confirmação lateral que vale registar

O painel mostra o número como **+55 79 9198-6091**, sem o nono dígito — exactamente
como o cabeçalho de `check-nono-digito-brasileiro.mjs` descreve. E na
`whatsapp_outbox` o destinatário está guardado com **13 dígitos** (`5579…`), a
forma longa.

As duas formas continuam a coexistir nesta conta, hoje. O guarda não protege
contra um episódio passado: protege contra uma condição que ainda é verdade.

---

# Fecho do dia — o portão de qualidade, corrido por mim

Não aceitei o "verify verde" de relato. Corri o `npm run verify` inteiro e li a
saída toda, não a cauda.

```
EXIT = 0
guardas que arrancaram : 69
guardas sem uma linha de saída : 0
build : ✓ em 2,88 s
páginas legais geradas : privacidade.html (12.163) e termos.html (7.626)
```

**São 69 guardas, não 70.** Contei os ficheiros `check-*.mjs` e os encadeados no
`verify`: os dois números batem em 69. Diferença sem importância prática, mas
prefiro o número que medi.

**Um susto meu, registado porque quase virou achado falso.** A minha primeira
contagem disse "69 arrancaram, 64 reportaram" e eu ia investigar 5 guardas mudos.
Fui verificar antes de escrever: **nenhum é mudo.** O meu padrão de busca é que
não apanhava as várias formas de dizer verde ("protecções ativas", "chaves
idênticas", "ficheiros conferidos"). Contei por bloco de saída em vez de por
palavra, e são 69 de 69 a falar.

## Risco 20 — o portão do lint está exactamente no tecto, sem folga nenhuma

```
eslint . --max-warnings 69
avisos contados na saída : 69
```

**Zero de margem.** O próximo aviso que alguém introduzir deixa o `verify`
vermelho — e o `AGENTS.md` já antecipa a reacção errada:

> *"Subir o tecto no `package.json` é a saída errada: apaga exactamente a medida
> que o portão existe para guardar. Se um aviso novo aparecer, corrige-se o aviso."*

Não é defeito: o tecto **é** a contagem real, por desenho. É risco porque a
pressão para o subir chega no pior momento — quando alguém tem pressa e o
`verify` fica vermelho por uma linha.

Entre os 69 estão os ficheiros acima de 350 linhas que já eram conhecidos (398,
428, 673, 363) e várias funções acima da complexidade 12.

## Os guardas novos do dia estão na corrente

`check:zero-nao-vira-um` (23 protecções nos 3 catálogos, o plural),
`check:recibo-do-whatsapp`, `check:nome-que-nao-existe` (25 ficheiros de função)
e `check:so-o-bilhete-reclama` correm todos dentro do `verify`.

## O limite de mensagens — respondido, e não é o gargalo

Print do Gestor do WhatsApp, "Limites de mensagens", actualizado a 05/09 11:44
GMT-3. **Leitura de imagem, não medição minha.**

```
escada : 250 → [2000 ATUAL] → 10.000 → 100.000 → Ilimitado

Atual: 2000 conversas iniciadas pelo negócio num período contínuo de 24 horas
```

**O tecto é 2.000 conversas por dia, não 250.** A conta está acima do escalão de
entrada, o que é evidência forte (não prova) de que a verificação do negócio está
feita — 250 é o escalão de quem ainda não a tem.

Para subir a 10.000, o painel diz o requisito: *"Começa conversas iniciadas pelo
negócio de alta qualidade com 1.000 clientes únicos num período contínuo de 7
dias."*

## E a Meta confirma, do lado dela, o que eu tinha medido do lado do banco

O mesmo painel diz:

> **"Iniciaste 1 conversas com clientes únicos nos últimos 7 dias."**

**Um.** É exactamente o que a `whatsapp_outbox` dizia: todas as mensagens que o
produto já enviou, em todos os canais e em toda a história, foram para um único
destinatário. Duas fontes independentes — o banco do produto e o painel da Meta —
com o mesmo número.

**Conclusão: a capacidade não é o gargalo, e nunca esteve perto de ser.** Há
2.000 conversas por dia disponíveis e o produto usou **uma** em sete dias. O que
falta não é limite da Meta: são clientes.

Isto encerra a linha de investigação que começou com a suspeita do número de
teste. Das três perguntas que substituíram a original:

| Pergunta | Estado |
| --- | --- |
| Escalão de mensagens | **Respondido: 2.000/dia.** Não é restrição prática |
| Verificação do negócio | **Provavelmente feita** — inferido de estar acima de 250, não confirmado na tela própria |
| App "Live" ou "Em desenvolvimento" | **Continua por ver.** Um app em desenvolvimento restringe a testadores mesmo com número e escalão bons |

A terceira é a única que sobra, e é a que ainda podia limitar o alcance.

## O WhatsApp, encerrado

Marcelo confirmou o último ponto: **App Mode: Live.** Não há restrição a
testadores.

A linha de investigação que começou com "o número ainda é de teste, limitado a 5
destinatários?" fecha assim:

| Pergunta | Resposta | Como se soube |
| --- | --- | --- |
| É número de teste? | **Não.** Número próprio, Ligado, qualidade Elevada | Print do WhatsApp Manager |
| Limitado a 5 destinatários? | **Não.** Campo "To" livre, não lista fechada | Print do API Setup |
| Tecto de mensagens | **2.000 conversas por dia** | Print de Limites de mensagens |
| Verificação do negócio | Provavelmente feita (inferido de estar acima de 250) | Inferência, não tela própria |
| App Live ou em desenvolvimento | **Live** | Confirmado por Marcelo |
| Nome de exibição | "Binno" **em revisão** — não impede enviar | Print do WhatsApp Manager |

**Do lado da Meta, nada impede o produto de alcançar um cliente hoje.** O que
falta são clientes: 2.000 conversas por dia disponíveis, **uma** usada em sete
dias — número que o painel da Meta e a `whatsapp_outbox` deram
independentemente.

A suspeita original era razoável e estava errada, e vale registar porquê: ela
nasceu de o banco mostrar um só destinatário. O mesmo facto tinha duas
explicações — "não consegue alcançar mais ninguém" e "ainda não teve mais
ninguém a quem escrever" — e o banco não distinguia entre as duas. Distinguiu-se
olhando para fora dele.

---

# Quebra 3 — metade fechada, medida em produção

`2d2fc15` no `origin/main`, pacote `index-BY4gCo0Y.js`, build de 15:30 UTC.

## O que fechou

```
/bem-vindo  SEM bilhete e sem sessão
  h1: "Não encontramos sua compra"
      "Esta página abre depois do pagamento. (…) Se você já pagou e caiu nesta
       tela, seu pagamento está registrado — fale com a gente e a gente libera
       seu acesso."
  308x44  "Ver o plano e assinar"
  308x44  "Já paguei — falar no WhatsApp"   (com o recado já escrito)
```

Duas saídas, ambas com **44px**. A régua do atrito 8 entrou no código novo — o
formulário antigo ao lado continua a 38 e 40.

E a segunda saída **arranha a quebra 2**: quem pagou e se perdeu passa a ter para
onde ir. Falta o outro lado, o toast que desaparece quando a reclamação falha.

## O que sobra, e é mais estreito

```
/bem-vindo?compra=cs_live_naoexisteqa1234567890
  h1: "Pagamento confirmado"
```

Inventei o bilhete: basta ter a **forma** certa. `BemVindo.tsx:109` decide por
`if (!bilhete && !user)` — "existe um bilhete" é tratado como "houve pagamento".

**Não é conserto verificar o bilhete**, e vale dizer porquê: naquele instante não
há sessão, e não existe porta anónima que diga se uma sessão do Stripe foi paga.
Criar uma seria pior do que o defeito. O que dá é a redacção — enquanto ninguém
confirmou, a tela pode dizer "vamos ligar o seu pagamento à conta"; depois de
`reclamar-compra` devolver sucesso, aí a frase é verdadeira.

**Rebaixada de quebra para atrito.** O que a fazia quebra — a tela afirmar
pagamento a quem não tem bilhete nenhum — está fechado e medido. Para ver o que
resta é preciso forjar o endereço, ou trazer um bilhete de uma sessão cujo
pagamento não vingou. Não é o caminho de ninguém a comprar de boa fé.

## Estado da decisão do Stripe

O Assessor escolheu o **ramo de pré-visualização do Supabase**, e a razão que ele
deu é a que eu tinha isolado: *a autorização do Marcelo cobre a troca, não a
reversão.* Falta ele confirmar se o ramo herda as Edge Functions e os segredos —
se não herdar, o ramo não sabe falar com o Stripe e a medição não acontece.

Deixei pronto na Área restrita, e continua válido em qualquer dos caminhos:

```
produto : prod_VCkqfVlMJxD7zQ   "Binno (teste QA)"
preço   : price_1UCLKk8mCDgFY7giKNhwZIim   R$99/mês BRL, livemode=false
```

## Google — o relógio, medido

```
ligação : 1, "connected", sem erro, criada 03/09 17:03
última sincronização : 03/09 23:15
avaliações : 10, última 04/09 17:43
```

A verificação **não foi submetida** — confirmado pelo Assessor, é fila dele. Com
o app em modo Teste a autorização expira em 7 dias, então o prazo é **~10/09**.

E fica registada a pergunta que ele aceitou medir: **o que a tela do dono mostra
quando a autorização expirar.** `status` é coluna de texto escrita quando a
ligação nasceu; se nenhum caminho de falha lhe tocar, o produto dirá "conectado"
com um token morto. É a mesma família do `accepted` do WhatsApp.

## Quebra 3 — FECHADA, medida em produção

`ae0d0c0`, pacote `index-C40ej_lh.js`, build 15:34 UTC. Com o mesmo bilhete
forjado que eu tinha usado:

```
COM bilhete forjado (cs_live_naoexisteqa1234567890)
  h1    : "Falta criar o seu acesso"
  corpo : "Vamos ligar o seu pagamento a esta conta."
  afirma "Pagamento confirmado"?  NAO
  ícone : lucide-key-round     (o visto verde saiu)

SEM bilhete — conferido que não regrediu
  h1    : "Não encontramos sua compra"
  308x44  "Ver o plano e assinar"
  308x44  "Já paguei — falar no WhatsApp"
  ícone : lucide-circle-help
```

O ícone importava: um visto verde afirma "confirmado" sem palavras, e sobrevive
a um conserto que só olha para texto.

## Atrito 8 — um botão em três telas, e é o formato de sempre

No mesmo ficheiro, três dos quatro alvos subiram para 44px. Ficou um:

```
308x44 ok       campo de e-mail
308x44 ok       campo de senha
308x44 ok       "Criar meu acesso"
308x38 PEQUENO  "Continuar com o Google"
```

`src/components/auth/BotaoDoGoogle.tsx:53` tem `py-2`, e o componente é usado em
**`Login.tsx`, `Signup.tsx` e `BemVindo.tsx`**. Uma linha ali sobe as três telas.

**Não é o formulário que ficou para trás: é a peça que as três partilham**, e que
nenhuma das duas passagens tocou porque em cada tela ela parece pertencer à
outra. É a mesma forma da quebra 4 (a regra do QR num chamador só) e da quebra 17
(o `rating` injectado pelo chamador): a regra aplicada tela a tela deixa de fora
o que as telas têm em comum.

## Uma regra que saiu de três guardas do mesmo dia

Ao consertar a quebra 3, o guarda do Assessor falhou de duas formas, e ele
contou-as: a fronteira do recorte usava como marco a frase "Pagamento
confirmado" — o próprio texto que o conserto removia —, e com o recorte vazio as
regras seguintes passavam todas, porque `!''.includes(...)` é sempre verdadeiro.

É a **terceira vez no mesmo dia** que a mesma doença aparece com roupa diferente
(a outra foi o `TS2304` que não apanhava o `TS2552`). Vale escrita como regra:

- **Uma asserção cuja pergunta deixou de existir devolve verdadeiro em vez de
  erro.** Todo recorte que alimenta asserções tem de falhar quando vem vazio.
- **Todo marco de recorte deve ser uma construção do código** — uma chave, um
  `return`, um nome de função — e nunca uma frase visível. Frase visível é
  exactamente o que os consertos mudam.

---

# Risco 21 — a ligação do Google quebra e ninguém é avisado

Pergunta do Marcelo, medida em vez de opinada.

## O que funciona, e corrige uma suspeita minha anterior

A coluna `status` **não** fica presa em `connected`:

```
sync-google-business-profile:243
  .update({ status: "revoked",
            last_error: tokenPayload.error || "Could not refresh Google authorization" })
```

Quando a renovação do token falha, a ligação passa a `revoked` com o motivo
gravado. O painel lê esse estado (`useGoogleBusinessReviewQueue.ts:53`), e o
guarda `check-aviso-diz-que-falhou.mjs` impede a tela de dizer "estamos
trazendo" quando na verdade falhou.

**Quem abrir o painel vê que quebrou.** Não é a família do `accepted` do
WhatsApp, e retiro a suspeita que eu tinha levantado.

## O que não existe

Medido na produção, três pontas:

```
calcular_saude_das_contas   olha google_business_connections?  NAO
                            olha last_error?                   NAO
sinal "coleta_antiga"       existe: "Última coleta há mais de 30 dias"
                            EXCLUÍDO do aviso, duas vezes:
                              where sinal <> 'coleta_antiga'
                            (20260903140000_o_aviso_fala_portugues.sql:62 e :93)
```

A ligação quebra, o banco sabe, a tela sabe — e **nenhum aviso sai**. Nem ao dono
nem ao Marcelo. O único sinal que apanharia isto indirectamente só acende aos
**30 dias**, e foi tirado do aviso diário de propósito, por razão correcta (mudava
de valor todo dia). Este caso ficou descoberto no meio.

E **a quebra é descoberta por tentativa, não por vigilância**: o `revoked` só é
escrito quando alguém manda sincronizar. Sem tentativa, nem a tela sabe.

## Por que importa, nas palavras do Marcelo

> *"Se a autorização expira e ninguém percebe, o cliente pagante fica dias sem
> receber avaliação nova e sem saber por quê. (…) cliente que paga e não recebe
> cancela, e ainda fala mal."*

Com o app do Google em modo Teste, a autorização expira a cada **7 dias**. A
ligação actual nasceu em 03/09 17:03 — o prazo vence por volta de **10/09**.
Deixa de ser hipótese na quarta-feira.

## O conserto que parece barato

O painel de administrador já lê o banco todo dia e já tem escada de sinais.
Falta olhar `google_business_connections.status <> 'connected'` e
`last_error is not null`.

E este é, pela regra do próprio contrato — *"cada sinal diz de quem é o
problema"* — um sinal **do Marcelo**: ninguém além do cliente pode reconectar. É
conversa com o cliente, não avaria a desentupir.

---

# A reconexão semanal NÃO exige a conta Google do lojista

Pergunta comercial mais cara em aberto: quantos clientes cabem, dado que a
autorização do Google expira a cada 7 dias no modo Teste. A leitura do Assessor
era que cada reconexão exige o lojista, o que punha o tecto em cerca de dez.

**O código não sustenta isso.**

## O que medi

```
start-google-business-oauth   exige sessão Binno + tem_acesso;
                              amarra o state a `user_id` (a conta BINNO)
google-business-oauth-callback:111   grava a ligação para `oauthState.user_id`
scope business.manage, access_type=offline, prompt=consent

sync-google-business-profile:253
  https://mybusinessaccountmanagement.googleapis.com/v1/accounts
  pageSize 20, com paginação completa   <- enumera TODAS as contas alcançáveis
```

**A identidade Google que autoriza é independente da conta Binno.** A conta Binno
decide de quem é a linha de ligação; quem estiver logado no Google naquele
instante decide quais perfis ficam alcançáveis. Nada no código amarra as duas.

## A consequência comercial

Se o Marcelo for **gestor** no Perfil da Empresa do cliente, ele faz a ligação e
cada reconexão semanal **sozinho**: entra na conta Binno do cliente, clica em
conectar, e autoriza com o Google dele. O lojista nunca precisa de estar
presente nem de dar palavra-passe.

O tecto deixa de ser *"dez clientes porque cada um tem de estar disponível"* e
passa a ser *"N vezes alguns minutos do Marcelo por semana"*.

## O que NÃO verifiquei, e aqui a distinção importa mais que noutro sítio

- **Que a API devolve perfis onde se é gestor e não dono.** É comportamento
  documentado do `accountmanagement` e é o propósito dela, mas não o corri contra
  um perfil de terceiro.
- A expiração continua a ser **por concessão**: continuam a ser N reconexões. O
  que muda é **quem** as pode fazer, não quantas.
- Cada cliente tem de aceitar adicionar o Marcelo como gestor. Isso é conversa de
  venda, não código.

## O que eu recusei responder

Se dá para tirar o app do modo Teste sem esperar a verificação: **não sei, e não
respondi de memória.** Responder de cabeça sobre regra de plataforma é o erro que
este relatório inteiro existe para não cometer.

O que dei foi a pergunta afinada, para a ida ao console ser decisiva: no ecrã de
consentimento são dois campos separados — *estado de publicação* (Testing / In
production) e *estado de verificação*. A pergunta certa é **o que acontece ao
mudar a publicação para "In production" com a verificação pendente**, e o que isso
faz ao ecrã que o lojista vê e ao prazo dos 7 dias. Estão os três na mesma página.

## A quarta vez, hoje, da mesma armadilha

O Assessor provou que o sinal novo `google_sem_sincronizar` dispara, correndo a
condição com limite de 1 dia. Sem isso teria um `false` que nunca poderia virar
`true` — indistinguível de saúde.

É a **quarta vez no mesmo dia** que a mesma doença aparece com roupa diferente:
o `TS2304` que não via o `TS2552`, a fronteira de recorte que usava como marco o
texto removido pelo conserto, o recorte vazio onde `!''.includes(...)` passa
sempre, e agora um sinal que podia nunca acender. Todas a mesma frase: **uma
verificação que ficou sem o que medir devolve "tudo bem" em vez de erro.**

---

# A home nova, medida a 390px

Subiu hoje e é o que todo prospecto vê. Até agora eu só lhe tinha confirmado o
`h1` para saber que tinha subido.

**Sem rolagem horizontal.** A imagem de prova carrega: `772x842` natural,
`complete=true`, desenhada a `358x390`.

*(Quase reportei que ela estava partida. Na primeira passagem li `natural=0x0` e
`alt=NAO` — mas ela é `loading="lazy"` e fica abaixo da dobra, então nunca tinha
sido pedida. Rolei a página inteira e ela carregou. Um `0x0` numa imagem lazy que
ninguém rolou não é defeito nenhum.)*

## Atrito 22 — a demonstração não está dobrada no celular, e a spec manda dobrar

`docs/nova-home-binno.md`, secção 6:

> *"no celular a demonstração fica dobrada atrás de um botão (`Abrir a
> demonstração`), e abre a pedido. Ela sozinha ocupava quase três telas de
> celular. No computador (a partir de 900px) abre sempre."*

Medido a 390px:

```
botão "Abrir a demonstração"        NAO existe
módulos do cockpit desenhados       5, inline
altura total da página              17.849px  ≈ 21 telas de celular
```

O cockpit inteiro é desenhado no telemóvel. É a única parte da spec da home que
não foi implementada como está escrita, e é justamente a que existia para a
página não ficar deste tamanho.

## Atrito 23 — a imagem que prova o produto não tem texto alternativo

`prova-avaliacao-google.jpg` é o print real do perfil da Noá no Google, e é a
prova da secção "Isso não é mockup". Ela vai ao ar **sem `alt`**.

Não é decorativa: é o argumento. Quem usa leitor de ecrã, ou quem esteja numa
ligação que não a traz, fica sem a única prova da página. A secção 5 da spec lista
correcções de acessibilidade a não regredir, e esta não estava lá — é omissão
nova, não regressão.

## Alvos abaixo de 44px na home: 15 de 33

Os que mais custam são os que a própria spec obriga a existir:

```
107x20  "Relatório Harmo"
130x20  "Mercado&Consumo"
 79x20  "Novo Varejo"
114x20  "Meio&Mensagem"
273x20  "Ler a política de avaliações do Google"
```

Os quatro primeiros são as **fontes obrigatórias** dos três números da secção "O
problema" — a spec exige que estejam visíveis e citadas por nome. Estão, e têm
20px de altura. O quinto é o link da política do Google na secção sobre review
gating.

Os restantes são os botões do cockpit da demonstração (Editar, Pular, Anterior,
Próxima, Copiar resposta), já contados no atrito 8 quando os medi em `/demo`.

---

# Correcção de método: eu media que existia, não que funcionava

O Marcelo apanhou, olhando a conta dele, que um cartão mostra os mesmos botões
para avaliação respondida e não respondida. **Eu não podia ter visto isso** — e a
razão é uma falha minha, não azar.

**Varri as seis telas autenticadas com uma conta de teste vazia**, e conta vazia
mostra estado vazio. E medi tamanhos e textos, **sem clicar em nada**. Um botão
que aparece do tamanho certo e não faz nada passava em tudo o que eu corri.

É a mesma armadilha que passei o dia a apontar nos guardas dos outros: verificar
que a coisa **existe** em vez de verificar que **funciona**.

## O que o achado é de verdade

Fui medir em vez de aceitar:

- `src/components/dashboard/ReviewsList.tsx` — o dos botões "Ver" e "Responder"
  sem `onClick` — **não é importado por ninguém.** Código morto, ~250 linhas.
- E `responseStatus` **está** definido: prop opcional declarada na linha 30 do
  próprio ficheiro, que nenhum chamador preenche. Não é referência inexistente.

O caminho vivo é `/settings` → `GoogleReviews` → `reviews/ReviewCard`, e ali o
comportamento **está correcto e documentado**: o cartão vem do retrato da Apify,
cego para quem já foi respondido, e a protecção é
`podeSugerirResposta={!temLigacaoOficial}` — com a ligação oficial viva deixa de
oferecer resposta, para o dono não responder duas vezes.

**O que sobra é custo, não defeito:** ele olha a lista e não distingue respondida
de não respondida. A protecção evita o dano e não devolve a informação. É decisão
de produto.

## O que fiz a seguir, sem depender de ninguém

O `/demo` é público e desenha o cockpit **com dados**. Dá para clicar nele agora.

```
Anterior          disabled=true no primeiro item, false depois de avançar  ✓
Próxima           a tela muda   ✓
Editar            a tela muda   ✓
Pular             a tela muda   ✓
Copiar resposta   a tela muda   ✓

chamadas a funções durante os cliques: NENHUMA
```

Os cinco botões do cockpit funcionam, e o `Anterior` está correctamente
desactivado no primeiro item — o que na primeira leitura pareceu erro de clique
era o botão a fazer o que devia.

E confirma-se a cláusula do contrato de que **a demonstração pública não paga
chamada nenhuma**: zero chamadas a funções durante toda a interacção.

## O que passa a valer para as próximas passagens

1. **Varrer com conta que tem dados.** Pedi ao Assessor uma conta de demonstração
   semeada — nomes e textos inventados, nunca copiados dos doze comentários
   reais, pela mesma razão que a área de administrador não mostra conteúdo de
   terceiros.
2. **Clicar.** Medir tamanho e texto não prova comportamento.

---

# Quebra 24 — a fila única não consegue mostrar as avaliações da ligação oficial

**Achado só possível com a conta semeada.** Com conta vazia esta tela mostra o
estado vazio, e o estado vazio esconde exactamente isto.

## O que a tela mostra

Conta de demonstração: **6 avaliações oficiais em `google_business_reviews`,
três delas sem `reply_text`** — ou seja, três à espera de resposta.

`/reviews`:

```
Fila de respostas   3 esperando resposta
Mensagens privadas  3      Comentários no Google  0     <-- zero
[três comentários privados]
```

`/dashboard`, na mesma conta e ao lado um do outro:

```
Reputação no Google     4,0 · 6 avaliações
                        5★ 50%  4★ 17%  3★ 17%  2★ 17%  1★ 0%
                        Tempo médio de resposta 19 h

Avaliações no Google    "Nenhuma avaliação buscada ainda.
                         Busque as suas avaliações do Google para montar a fila."
```

Na mesma dobra: *"tem 6 avaliações, média 4,0, e aqui está a distribuição delas"*
ao lado de *"nenhuma avaliação buscada ainda"*.

## A causa, medida no código

```
useFilaDeRespostas.ts:43     .from('google_reviews_awaiting_reply')   <- ÚNICA fonte Google
sync-google-business-profile  ocorrências de google_reviews_awaiting_reply: 0
experimentalApifyCollection   ocorrências: 2  (é quem a escreve)
```

`google_reviews_awaiting_reply` é escrita **só** pela coleta da Apify. A
sincronização oficial **nunca escreve nela**. E a fila de `/reviews` lê **só**
essa tabela para o lado do Google.

**Logo: uma avaliação que chega pela ligação oficial nunca aparece em
`/reviews`.**

`Reviews.tsx` não importa `useGoogleBusinessReviewQueue` — o tipo
`OrigemDaResposta` tem `'google-oficial'`, mas nada nesta página o produz.

## Por que isto contradiz o contrato em dois sítios

**"Uma fila só para responder"** (30/08) diz que a página soma as três origens,
e nomeia a segunda: *"a fila oficial do Perfil da Empresa
(`GoogleBusinessReviewQueue`)"*. Ela não está lá.

**"Uma fonte para o Google"** (03/09) diz que, com retrato oficial completo, a
fonte oficial *"é a única que fala pelo Google"*. Se a oficial passa a mandar e a
fila só lê a da Apify, a fila fica permanentemente a zero para o Google.

E o guarda que nasceu disso foi escrito porque **o Marcelo viu 6 numa tela e 8
noutra, no mesmo momento**. Hoje vê 6 numa e 0 noutra.

## O que eu não consigo distinguir, e é honesto dizer

A conta de demonstração tem ligação oficial semeada e **nenhuma coleta Apify**.
Uma conta real hoje — a da Noá — tem as duas, e ali a fila enche-se pelo lado da
Apify e parece correcta.

Então **não sei** se isto já morde alguém hoje, ou se só morde quando a Apify sair
e a oficial ficar sozinha — que é a direcção que o produto tomou. O que está
medido é que o caminho oficial não tem porta para esta fila.

## Como reproduzir

1. Conta com linhas em `google_business_reviews` sem `reply_text`, e nada em
   `google_reviews_awaiting_reply`.
2. Abrir `/reviews`: "Comentários no Google 0".
3. Abrir `/dashboard`: a reputação conta as mesmas avaliações.

## E uma coisa que passou, medida com dados

**Nenhuma função paga dispara sozinha, nem com a conta cheia.** Interceptei as
sete portas pagas no navegador e varri as cinco telas: **zero** chamadas.
Abrir o produto não gasta dinheiro — agora medido com dados, e não só com conta
vazia.

---

# A compra rodou de ponta a ponta — e o que a medição mostrou depois

**21:03 de 05/09.** A lacuna que atravessou o dia inteiro fechou.

```
21:03:11  pago_em       webhook grava em compras_a_reclamar  (a PRIMEIRA linha
                        na vida do produto — estava a zero desde sempre)
21:03:40  reclamada_em  o bilhete liga a compra à conta nova, 29 segundos depois
                        assinatura criada, eligibility_status "verified"
21:08:41  cancelada     e tem_acesso passa a false
```

Sem dinheiro: `amount_subtotal` 9900, **`amount_total` 0** (cupom de 100%),
`payment_status: paid`, `status: complete`, e zero cobranças no Stripe.

**As cinco pontas ligaram:** pagamento → webhook → fila de compras → conta nasce
→ acesso abre → cancelar fecha.

## Quebra 25 — `processed_at` não diz a verdade, nas duas direcções

Medido na `billing_webhook_events`:

```
21:06:20  customer.subscription.deleted    processado=SIM   erro=null
                                           …e a assinatura ficou `active`
21:03:11  checkout.session.completed       processado=NAO   erro=null
                                           …e a compra foi gravada com sucesso
```

**Um evento marcado como feito que não fez, e um evento feito que não foi
marcado.** O mesmo campo, errado nos dois sentidos, no mesmo minuto.

O primeiro é o defeito que o Assessor corrigiu em `efd7f90` — o webhook
procurava o dono só em `metadata.user_id` e `client_reference_id`, que só
existem para quem já tinha conta.

**O segundo é por construção, e ainda está lá.**
`stripe-billing-webhook/index.ts:83` — o ramo sem conta faz
`return json({ received: true, sem_conta: true })`, e **volta antes** da linha
214, que é onde `processed_at` é escrito.

Logo: **toda compra sem conta deixa o seu `checkout.session.completed` marcado
como não processado**, mesmo tendo corrido bem. E compra sem conta é o caminho
que o produto passou a vender a toda a gente.

**Por que importa.** Sozinho não estraga nada — mas quem depurar vai ler "não
processado" e concluir que falhou. E se alguém construir alarme ou repetição
sobre `processed_at is null`, **toda compra bem sucedida vira alarme**.

## O que dá para provar do conserto do cancelamento, sem gastar

O conserto passa a procurar o dono por `stripe_subscription_id`. Essa chave só
serve se existir e casar. Medido:

```
compras_a_reclamar.stripe_subscription_id   presente
subscriptions.stripe_subscription_id        presente
os dois batem                               sim
stripe_customer_id                          presente
assinaturas sem id do Stripe, na tabela     0
```

A dependência de dado do conserto está satisfeita. **O que continua por provar é
o evento a chegar e a produzir efeito** — isso precisa de um cancelamento real,
e a assinatura de hoje já foi cancelada à mão às 21:08:41, dois minutos depois do
evento.

## CORRIGIDO DUAS VEZES — e a segunda correcção é a que importa

**A minha primeira correcção também estava errada.** Deixo as duas por escrito,
na ordem em que aconteceram, porque o percurso é o achado.

**O que se passou de facto:** às 21:04 o Assessor mediu e os dois e-mails ERAM
diferentes (`hotmail` na compra, `homtail` na conta). Às **21:06:33** ele alterou
a linha de `auth.users` para os fazer bater, porque o Marcelo dissera ser dono do
endereço. Eu medi depois disso e li o resultado da correcção dele.

Confirmei a marca de tempo, que é o que resta:

```
conta criada    21:03:39.207
linha alterada  21:06:33.376     <- 2 min 54 s depois, única alteração
```

**O erro dele:** apagou a anomalia antes de ela estar explicada. Viu uma
divergência, contou-a como prova, e três minutos depois sobrescreveu-a por
arrumação. A anomalia era o dado; foi tratada como sujidade. E isso transformou um
desacordo que se resolvia por medição num que já não se resolve.

**O erro meu, e é o que levo do dia todo:** medi o estado ACTUAL e concluí sobre o
PASSADO. Escrevi "os dois e-mails são idênticos, logo sempre foram" — e
`updated_at` estava a uma coluna de distância. **Num sistema onde agentes
escrevem em produção, estado actual não é histórico.**

**A conclusão sobrevive às duas correcções, por um terceiro caminho:** a compra de
hoje não serve como prova do conserto de ontem — não porque os e-mails fossem
iguais, mas porque **já não é possível estabelecer que eram diferentes**. Um facto
que não se pode voltar a verificar não prova nada.

### O que fica escrito como regra

- **Não arrumar uma anomalia antes de a explicar.** Registar e corrigir são as
  duas coisas a fazer, e a ordem não é indiferente.
- **Ao medir produção, perguntar se o dado mudou.** `created_at` contra
  `updated_at` custa uma coluna e separa observação de arqueologia.

---

## (Primeira correcção, mantida por honestidade — e ela própria errada)

Escrevi aqui que o Marcelo tinha errado o e-mail (`homtail` em vez de `hotmail`)
e que a compra ligara na mesma, e chamei-lhe a melhor prova do dia. **Era falso.**

Ele corrigiu: usou outro e-mail dele, e não houve erro nenhum. Fui medir:

```
domínio no Stripe : hotmail.com
domínio na conta  : hotmail.com
os dois e-mails    IDÊNTICOS
```

Logo a compra teria ligado **de qualquer maneira** — pelo bilhete ou pelo e-mail.
Aquela compra não demonstrou nada sobre o conserto de "só o bilhete reclama".

**O que continua verdade:** esse conserto está provado pelo código e pelo guarda
(`reclamar_compra` devolve nulo sem bilhete, migração lida). O que ele não tem é
prova por uso real, e a compra de hoje não serve como tal.

**A lição, e é minha:** o Assessor inferiu um erro de digitação de uma string que
pareceu estranha, eu repeti como prova, e nenhum de nós mediu. A resposta estava a
uma consulta de distância. É a mesma doença do dia — afirmar o que não se mediu —
desta vez do lado de quem a andou a apontar.

## Uma pendência que a compra revelou

A assinatura nova nasceu com `current_period_start` e `current_period_end`
**nulos**, enquanto a de 04/09 tem as duas preenchidas. Não tranca acesso — a
regra trata nulo como "não expira" — mas é exactamente o que a aba de cobrança
precisa para dizer *"activa até tal dia, renova em tal dia"*, que foi a queixa
que fez nascer o guarda da porta da assinatura.
